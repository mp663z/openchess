# ai-sdk

BYOK provider-neutral model SDK. One interface across providers; no
provider names leak into call sites. Prompts, retries, cost tracking, and
redaction live here. No model output reaches the user without the
evidence-gated task that owns the claim.
