# T0002 Register H1-H5 - evidence

Register: validation/hypotheses-register.yaml
- All five hypotheses locked with exact cohort, pass/fail thresholds (verbatim from product report v5 section 6), measurement query, owner (role only: product owner) and locked date 2026-09-19.
- H2 carries the v5 section 8 restatement: concierge phase is directional only and cannot pass/fail H2.
- H5 recorded as demoted (signal, not gate) per the report.
- External timestamp evidence: first merge commit of this file on the public repo (same pattern as T2289).

Verification: independent review required - routed to the verifier with the merge SHA after PR merge.
Commands: `pytest tests/test_hypotheses_register.py -q`. Environment: python 3.11, PyYAML.
