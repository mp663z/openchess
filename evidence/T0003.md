# T0003 Keep placeholder name - evidence

Policy: docs/governance/naming.md. Mechanism:
- brand/__init__.py defines PRODUCT_NAME = "{{PRODUCT_NAME}}" as the single source; all user-visible naming imports it.
- tests/test_naming.py enforces: (a) the constant is the literal placeholder; (b) no *.py assigns an alternative PRODUCT_NAME/APP_NAME/TITLE; (c) the working codename "OpenChess" may not appear in tracked code/config outside an explicit prose allowlist (tools/dag.py docstring) and prose dirs (docs/, evidence/, data/, tasks/).
- Name gate remains an owner decision; adopting a real name is one constant change + test update.

Verification: independent review - routed to the verifier with the merge SHA.
Commands: `pytest tests/test_naming.py -q`. Environment: python 3.11, stdlib only.
