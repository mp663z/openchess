# T0003 Keep placeholder name - evidence (v2, remediated)

Policy: docs/governance/naming.md (v2). Mechanism:
- brand/__init__.py: PRODUCT_NAME = "{{PRODUCT_NAME}}" single source for code.
- User-visible surfaces use the placeholder directly: README heading + promise and tasks/dag.json product_promise now read {{PRODUCT_NAME}}; data/datasets/public-source-rights.yaml and docs/licensing.md likewise.
- tests/test_naming.py (v2): case-insensitive scan across ALL tracked text files (py/md/yaml/yml/toml/json/txt) with an explicit, justified allowlist (pyproject distribution slug, repo URLs, the two verbatim docs/plan source documents). No directory exemptions. Separate tests: no alternative name assignment anywhere; user-visible surfaces use the placeholder.

Verifier history: v1 (d5f8d2f1) FAIL - README used the codename publicly; pyproject slug unscanned (case-sensitive); dag.json product_promise and rights yaml hardcoded the codename behind directory exemptions; evidence overstated consumer coverage; scan too narrow. All five fixed: README/dag.json/rights-yaml now placeholder, pyproject slug is an explicit justified exception, evidence v2 states the true mechanism, scan is case-insensitive + Markdown-inclusive + allowlist-based, and test_user_visible_surfaces_use_placeholder covers the real surfaces.

Verification: independent review - routed to the verifier with the v2 merge SHA.
Commands: `pytest tests/test_naming.py -q`. Environment: python 3.11, stdlib only.
