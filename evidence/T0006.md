# T0006 No silent writes - evidence

Invariant + reference implementation: docs/governance/no-silent-writes.md, brand/approval.py.

## Trail correction (required by independent review)

The v1 files (brand/approval.py, docs/governance/no-silent-writes.md,
tests/test_no_silent_writes.py, evidence/T0006.md) were merged to main
ACCIDENTALLY via PR #26 at d62a0b45, swept in by a `git add -A` during the
T0003 v2 remediation. There was no dedicated T0006 branch or PR; any earlier
implication of one is wrong. Remediation lands through this scoped T0006 v2 PR.

## v1 failed independent review (d62a0b45)

Real bypass: approve() trusted the caller-supplied proposal["diff_hash"]
without recomputing; commit() only checked hash presence in _approvals; the
hash excluded `action`; approver_role was any caller-supplied string. The
verifier reproduced an approve-then-mutate replay that audited a destructive
action under an approved hash, and fabricated dicts with stolen hashes.

## v2 failed independent review (47eb9222)

Artifact binding confirmed closed, but the registry authenticated identity
without AUTHORIZING role-per-action: a reviewer-role identity approved a
destructive delete_all_user_data. Verifier repro: ApprovalGate({'review-bot':
'reviewer'}) -> approve delete_all_user_data as review-bot -> commit
succeeded.

## v3 remediation

- Actions map to required capabilities (default user-data-write, fail-closed;
  delete_game/delete_games/delete_all_user_data map to user-data-destructive).
- Only the `user` role holds capabilities; reviewer/service/admin hold none.
- Destructive capability requires the user role directly and CANNOT be
  delegated by any grant.
- CapabilityGrant: user-role grantor delegates one exact (action,
  target-scope) non-destructive capability; exact target or prefix/* subtree;
  optional expiry; non-user grantors confer nothing; no widening.
- Authorization decision ("role:user" / "grant:<grantor>") is bound into the
  Approval record and the audit log.
- Registry policy stated in docs/governance/no-silent-writes.md: registry
  entry only via reviewed governance PR; membership confers only the role's
  table capabilities.

Tests (tests/test_no_silent_writes.py, 25): adds reviewer/service/admin
cannot approve user-data writes, destructive requires user, destructive
undelegatable, scoped grant allows exact action+target, grant cannot widen
action or target, non-user grantor confers nothing, expired grant confers
nothing, authorization bound into approval+audit, unknown action defaults to
user-only.

## v2 remediation

- Immutable typed Proposal (frozen dataclass): action + target/scope +
  before/after. There is no caller-supplied hash field at all.
- The hashed artifact covers the FULL proposal; canonical JSON serialization,
  no default=str (unserializable payloads raise, fail-closed).
- approve() recomputes canonical + sha256 from the live proposal,
  constant-time compares (hmac.compare_digest), and stores the canonical
  artifact bytes - not just the hash.
- commit() revalidates type, recomputes canonical + sha256, and
  constant-time compares BOTH the hash and the stored canonical bytes; the
  committed proposal must be byte-identical to the approved artifact.
- Approvers are a registry (identity -> role); unregistered identities are
  rejected and the recorded role always comes from the registry.
- Approvals remain single-use; every commit lands in the audit log with
  identity, role, action, target, and artifact hash.

Tests (tests/test_no_silent_writes.py, 13): unapproved write raises; approved
write commits and audits; binding to exact diff; action in artifact;
target/scope in artifact; before/after mutation with stale hash rejected;
fabricated-hash dict rejected; malformed proposals rejected; invalid approver
rejected + registry-sourced role; gate requires approvers; single-use;
audited artifact byte-identical to approved; approval record immutable.
Commands: `pytest tests/test_no_silent_writes.py -q`. Environment: python 3.12, PyYAML.

Recorded merge SHA on main: e6bbb77b17dbd19d86a42f9fa02e106049f7d161

Status: done
Verification: independent review - PASS at e6bbb77b17dbd19d86a42f9fa02e106049f7d161
