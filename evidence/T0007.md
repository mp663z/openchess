# T0007 Evidence contract - evidence

Contract: docs/governance/evidence-contract.md - done requires acceptance + exact SHA + verifier manifest; UNVERIFIED is never done.
Enforcement: tools/evidence_lint.py lints every evidence/*.md (title names task, exact 40-hex SHA or `pending`, Verification: line, Commands: line, done-claims require SHA); wired into CI and the pre-push hook; dag.py complete already refuses without --sha/--evidence. Independent-review tasks are done only after a verifier PASS against the exact merge SHA.
Backfill: 23 pre-contract files carry `pre-contract: true` markers with retroactive verification-mode index from tasks/dag.json (no fabricated content); 7 recent files gained their full recorded merge SHAs.
Tests (v1, superseded; v2 file is tests/test_evidence_lint.py): full file passes, pending marker, missing SHA, missing Verification, missing Commands, done-without-SHA, pre-contract grandfathering, title check, repo tree lint-clean.

## v2 - remediation of independent-review FAIL at 96ea9e02

Bypasses closed:
1. Grandfathering is frozen: data/evidence-pre-contract.yaml pins the 23 paths to sha256-of-bytes; the marker must be an exact footer line; prose mentions and non-allowlisted files fail; any edit invalidates.
2. Status is a strict field (done|in-progress|pending); prose completion claims and stray "pending" words mean nothing.
3. Verification mode must equal the board's mode from tasks/dag.json; done independent/human tasks need `PASS at <sha>` equal to the board done_sha; UNVERIFIED verdicts fail.
4. Commands must include a backticked command AND an Environment record.
5. Done requires `Recorded merge SHA on main: <40-hex>` equal to tasks/dag.json done_sha (also fixed T0004's short board SHA to the full 784e4eccd06eb36b181c620e88d39cbda067fd52).
6. Test file renamed to tests/test_evidence_lint.py; the old grandfathering test that blessed substring markers is replaced by adversarial fixtures for every bypass above.

## v3 - remediation of independent-review FAIL at a3e2da9e

v2's frozen-grandfathering claim did not hold; v3 closes the remaining bypasses:
1. Allowlist pinned in trusted code: tools/evidence_lint.py hardcodes ALLOWLIST_SHA256 (sha256 of the allowlist file's exact bytes). Editing data/evidence-pre-contract.yaml (entry swap preserving count, digest retarget) fails closed: nothing is grandfathered and the tamper is an explicit error. Changing the allowlist now requires changing enforcement code.
2. Marker must close the file: only whitespace may follow `pre-contract: true`; trailing content re-subjects the file to the full contract.
3. An evidence file whose task is missing from tasks/dag.json is an error; a board task without a verification mode is an error.
4. Commands must contain a nonempty backticked command (no `` or ` ` or lone backtick) and a nonempty Environment value.

Tests (tests/test_evidence_lint.py, 22): the 14 v2 tests (one re-aimed: edit-above-marker hits the digest path; appended content now hits the EOF rule) plus 8 v3 adversarial tests - exact 23-entry pinned mapping, allowlist swap preserving count fails closed, digest constant covers current file, marker-before-trailing-content, trailing-whitespace-only still grandfathers, unknown task, empty/lone/blank backticks, empty environment.

v2 tests (superseded by v3 additions): full file passes, prose-marker mention, non-allowlisted marker, edit-invalidates, prose pending, done-without-board-done, wrong SHA, short SHA, banana mode, missing/mismatched PASS-at, UNVERIFIED verdict, commands+environment, repo tree clean, frozen allowlist shape.
Commands: `python tools/evidence_lint.py && pytest tests/test_evidence_lint.py -q`. Environment: python 3.12, PyYAML.

Status: done
Verification: independent review - PASS at 097782c2406ec2e43358d4f916fd04bdf61c055d (v3; verifier replayed every v1/v2 bypass: replacement attack fails closed, EOF marker enforced, ghost task rejected, empty backticks/environment fail; sole caveat is the architectural trusted-code change path, not a defect)
Recorded merge SHA on main: 097782c2406ec2e43358d4f916fd04bdf61c055d
