# T0010 CLA - evidence

Acceptance: CLA lint passes and designated owner/counsel approves.

Artifact: CLA.md - contributor license agreement: definitions; irrevocable copyright grant (sublicensing limited to AGPL-3.0-or-later or a governance-approved license); patent grant with litigation-termination clause; contributor representations (originality, employer rights, AS-IS, third-party material identification for the fail-closed license audit); no-support clause; outbound AGPL-3.0-or-later with network source offer consistent with NOTICE; acceptance via automated CLA check, de minimis contributions under DCO.md instead.
Lint: tools/governance_doc_lint.py; tests/test_t0010_cla.py asserts lint-clean plus both grants, irrevocability, AGPL outbound, and the patent-litigation clause.
## v2 - remediation of batch FAIL at 1f86ee5

1. Sublicensing locked: section 2 now grants sublicensing ONLY under AGPL-3.0-or-later and explicitly grants no right to relicense; the open-ended governance-approved-license option is removed (test asserts both directions).
2. Acceptance mechanism made real: section 7 no longer claims an automated CLA check. Acceptance = contributor's stated acceptance in their first PR, recorded by a maintainer in data/cla-acceptances.yaml (created, schema_version 1); CI validates the registry; no registry entry, no merge (de minimis per DCO.md excepted).
3. Shared lint v3 inherited from T0009 (block-state-aware headings, nonempty bodies); CLA required headings registered; mutation test for a dropped grant heading.

## v3 - remediation of FAIL at 15252258 (registry enforcement not real)

1. tools/cla_check.py: PR gate reading the author from the trusted GitHub event payload (GITHUB_EVENT_PATH), changed lines from the payload, and the real diff via git name-status. De minimis iff <=10 lines AND no added/removed/renamed file AND no protected path (governance docs, tools/, data/, tasks/, manifests); anything else requires a registry entry. Wired as a pull_request CI step.
2. Strict registry validation: normalized GitHub handle charset, ISO-8601 timestamp with timezone, URL or owner/repo#N reference, unique handles; malformed registry fails closed.
3. Tests (9): real registry validates, bad handle / non-ISO timestamp / duplicate / bad reference rejected, de minimis classification bounds, registered vs unregistered author gate, de minimis pass, CI wiring assertion.
4. CLA.md section 7 names the actual mechanism (tools/cla_check.py).

v4 - remediation of FAIL at 754a378f (self-authorization + syntax-only reference checks):

1. Registry loaded from the trusted BASE ref via git show (load_base_registry), never the PR head; check_pr uses it exclusively. An entry added in the PR itself cannot authorize its author - test_pr_gate_self_entry_in_current_pr_does_not_authorize reproduces the attack as a failing case.
2. References restricted to this repository's PRs (owner/repo#N or its github.com URL; repo from GITHUB_REPOSITORY, default the canonical repo); other repos/hosts rejected.
3. verify_acceptance: referenced PR must be authored by the registered handle AND contain the exact statement "I accept the CLA in CLA.md" in body or an own comment (GitHub API via gh, GH_TOKEN); unverifiable entries fail closed.
4. accepted_at parsed as a real timezone-aware datetime (2026-99-99 and naive values rejected), not just regex shape.
5. Tests (10): timestamp reality, repo-scoped references, handle/duplicate, verification paths (body/comment/wrong-author/foreign-statement), de minimis bounds, registered-vs-unregistered gate, self-entry attack, CI wiring.

Human part: owner delegated judgment for this governance chain 2026-09-19 04:28 IST over WhatsApp (wamid.HBgMOTE4MTIxNzk4Mjg1FQIAEhgUM0IxQzU0M0UzNDg2NDU1NDdERDUA, "check and merge with your own judgement", chain-scoped). Independent-verifier PASS still required before merge.

Commands: `python tools/governance_doc_lint.py CLA.md && pytest tests/test_t0010_cla.py -q`. Environment: python 3.12.
Status: in-progress
Verification: auto + human - lint built and green; owner approval routed via parent
