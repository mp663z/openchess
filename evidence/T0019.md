# T0019 Private control-plane repo - evidence

Acceptance: Separate history, CI, deployables and credentials.

Repository: https://github.com/mp663z/openchess-control-plane (GitHub visibility: PRIVATE). Fresh git init, no fork or shared objects with the public repo - separate history.
Layout: seven component areas matching exactly the private_side_only areas of the public repo's licensing-boundary.yaml: identity, billing, entitlements, quota, provider-routing, abuse-admin, ops. Nothing chess-related lives there (AGPL boundary).
Separate CI: .github/workflows/ci.yml in the private repo - boundary guard (any top-level directory outside the allowed private_side_only areas fails the build) plus deployable check (entrypoint importable, Dockerfile pins it, build script parses). Green at external commit 3d530c59e7d256ff26be85df9081c265833b38bc (workflow run 35418669395 on https://github.com/mp663z/openchess-control-plane).
Separate deployable: single container image (Dockerfile, python:3.12-slim, stdlib-only entrypoint ops.health_server exposing /healthz and /readyz), built by ops/build.sh and tagged with the exact commit SHA. Documented in ops/DEPLOY.md.
Separate credentials: no credentials shared with the public repo. Read-only deploy key "control-plane machine pull (read-only)" registered on the private repo (created 2026-09-19T03:31:22Z, read_only=true); the private half exists only as the Actions secret CONTROL_PLANE_DEPLOY_KEY on https://github.com/mp663z/openchess-control-plane for future deploy workflows. No PATs, no user credentials, no secrets in the tree.

Provenance note: this task's work product is the private repository itself, whose history is intentionally separate (the acceptance criterion), so the external merge commit 3d530c59e7d256ff26be85df9081c265833b38bc on https://github.com/mp663z/openchess-control-plane cannot be reachable from this repo's main. The recorded SHA below is instead the public-repo commit that lands this evidence file - the DAG reconcile anchor - while the external commit above is the actual work-product provenance, verifiable in the private repo.

Commands: `git clone https://github.com/mp663z/openchess-control-plane.git && python3 -c "import ops.health_server" && bash -n ops/build.sh`. Environment: python 3.12.
Status: in-progress
Verification: auto - in-progress; private-repo CI green at external commit 3d530c59e7d256ff26be85df9081c265833b38bc (boundary guard + deployable check, run 35418669395); independent verifier review of the control-plane state requested via parent
