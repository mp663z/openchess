# T0020 Task/evidence registry - evidence

Acceptance: rejects duplicate IDs, cycles, empty checks and done without evidence.

Registry: tools/dag.py + tasks/dag.json (5,257 tasks). Rejection classes:
- duplicate IDs: index() raises DagError on any duplicate (fail-fast at load, before any verify);
- cycles: Kahn topological check in verify() ("dependency cycle detected");
- empty checks: verify() rejects empty/whitespace title, acceptance, verification fields ("empty <field>");
- done without evidence: verify() requires done_sha AND evidence_manifest on every done row; cmd_complete refuses without --sha/--evidence.
Tests (tests/test_dag.py, 11): real board valid (5,257 pinned), claimable respects dependencies, complete requires SHA+evidence, complete blocked by unmet dependency, cycle detected, done without SHA flagged, duplicate IDs raise, empty acceptance/verification/title rejected.

Commands: `python tools/dag.py verify && pytest tests/test_dag.py -q`. Environment: python 3.12.
Status: in-progress
Verification: auto - built; routed after chain position (deps T0017)
