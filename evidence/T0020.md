# T0020 Task/evidence registry - evidence

Acceptance: rejects duplicate IDs, cycles, empty checks and done without evidence.

Registry: tools/dag.py + tasks/dag.json (5,257 tasks). Rejection classes:
- duplicate IDs: index() raises DagError on any duplicate (fail-fast at load, before any verify);
- cycles: Kahn topological check in verify() ("dependency cycle detected");
- empty checks: verify() rejects empty/whitespace title, acceptance, verification fields ("empty <field>");
- done without evidence: verify() requires done_sha AND evidence_manifest on every done row; cmd_complete refuses without --sha/--evidence.
Tests (tests/test_dag.py, 11): real board valid (5,257 pinned), claimable respects dependencies, complete requires SHA+evidence, complete blocked by unmet dependency, cycle detected, done without SHA flagged, duplicate IDs raise, empty acceptance/verification/title rejected.

v2 remediation (FAIL at 179d6ed1): exact-SHA contract enforced - cmd_complete and verify require a full lowercase 40-hex SHA (require_sha) and evidence_manifest == evidence/<task-id>.md exactly (require_evidence). Adjacent-wrong tests: short/nonhex/uppercase/overlong SHA, empty/wrong/missing/other-task evidence path, verify() flags lax done records. Legacy board data normalized in the same PR: T2231-T2236 manifests pointed at the existing per-task files; evidence/T0001.md and evidence/T2289.md created (original manifests scope/v0.1.yaml and validation/prereg-h0.yaml referenced inside; original independent-verifier PASS at 92d31111 recovered from the verifier transcript); T3761 done_sha expanded to full 40-hex.

Commands: `python tools/dag.py verify && pytest tests/test_dag.py -q`. Environment: python 3.12.
Status: done
Recorded merge SHA on main: cdb4ad45075d6f0d2ceeeb8f142e3e7c603642fa
Verification: auto - PASS at cdb4ad45075d6f0d2ceeeb8f142e3e7c603642fa (independent verifier, PR head c261527b142b299b013113ef46623010daaf251f); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
