# T0021 Install format/type/API check - evidence

Acceptance: format/type/API - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: tools/install_checks/ harness (discovery by filename, no central registry - parallel lanes cannot conflict on a shared file) + check_format_type_api.py (ruff clean + pinned public API via AST) + fixtures seed_good.py/seed_bad.py (lint error + unpinned export). Runner: tools/install_checks/runner.py runs every check in good AND violation mode; wired into CI. T0032 shares the harness.
Tests (tests/test_install_checks.py): dual-mode green, discovery, violation caught for both checks, CI wiring, fixtures tracked.
Commands: `python -m tools.install_checks.runner && pytest tests/test_install_checks.py -q`. Environment: python 3.12.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
