# T0021 Install format/type/API check - evidence

Acceptance: format/type/API - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: tools/install_checks/ harness (discovery by filename, no central registry - parallel lanes cannot conflict on a shared file) + check_format_type_api.py with THREE INDEPENDENT detectors: format (ruff), type (mypy --strict, pinned mypy>=1.10,<2.0 in requirements-dev.txt), API (pinned public API via AST, exact set match). Seeded fixtures: seed_good.py (clean under all three) and one violation fixture per detector - bad_format.py (ruff F401 only), bad_type.py (int+str type error only), bad_api.py (unpinned extra export only). Violation mode requires each fixture to trip EXACTLY its intended detector, so a missing or neutered detector fails the harness. Runner: tools/install_checks/runner.py runs every check in good AND violation mode; unknown check IDs and empty discovery fail closed; wired into CI.
Tests (tests/test_install_checks.py): dual-mode green, discovery, unknown-ID fail-closed, per-fixture exact-detector assertions, monkeypatch mutation tests (removing each detector fails the harness), CI wiring, fixtures tracked, mypy pinned as a real dev dependency.
Commands: `python -m tools.install_checks.runner && pytest tests/test_install_checks.py -q`. Environment: python 3.12.
Status: done
Verification: auto + fault injection - PASS at cbb5998d854632a015d78daaedd6ba6f3cb5094a (independent verifier, PR head 1e94c534880108d27ea570d5fdfa40ca5212edd5)
