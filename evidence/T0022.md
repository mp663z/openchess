# T0022 Install unit/contract check - evidence

Acceptance: unit/contract - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: tools/install_checks/check_unit_contract.py wraps the REAL board verifier contract (tools/dag.py verify). Good mode: the real tasks/dag.json (5,257 tasks) verifies clean. Violation mode: 9 seeded board fixtures under tools/install_checks/fixtures/dag/, each breaking exactly one contract rule - dependency cycle, unknown dependency, duplicate id, null dependencies, null id, empty id, nonstring field, invalid status, missing required field - and each must be REJECTED by the real verifier with its own seeded reason. Failing closed is part of the contract: verify() must report problems, never crash (a crash on a malformed board counts as uncaught). As part of this task, dag.verify was hardened from crash-on-malformed to strict fail-closed validation: tasks must be a list; each entry a mapping with a non-empty string id (duplicates rejected); dependencies must be a list of strings (null/non-list/nonstring entries reported, not crashed on); title/acceptance/verification must be strings (type-coercion no longer masks nonstring fields); cycle detection runs only over well-formed entries. This also closes the T0020 verifier residual (dependencies:null crash, id/type strictness). Regression tests: tests/test_dag.py (7 new strictness tests + updated duplicate-id test asserting both the verify problem and the claimable/index fail-closed raise).
Commands: `python -m tools.install_checks.runner T0022 && python tools/dag.py verify`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
