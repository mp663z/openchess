# T0022 Install unit/contract check - evidence

Acceptance: unit/contract - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: tools/install_checks/check_unit_contract.py wraps the REAL board verifier contract (tools/dag.py verify). Good mode: the real tasks/dag.json (5,257 tasks) verifies clean. Violation mode: 18 seeded board fixtures under tools/install_checks/fixtures/dag/, each breaking exactly one contract rule - dependency cycle, unknown dependency, duplicate id, null dependencies, null id, empty id, nonstring field, invalid status, missing required field, non-mapping board, unhashable status (list), duplicate dependency edge, null metadata field, integer done_sha (type-coercion into a valid-looking SHA), missing/wrong schema_version, empty tasks list, whitespace-padded id - and each must be REJECTED by the real verifier with its own seeded reason. Failing closed is part of the contract: verify() must report problems, never crash (a crash on a malformed board counts as uncaught). As part of this task, dag.verify was hardened from crash-on-malformed to strict fail-closed validation: the board must be a mapping with schema_version exactly '5.2-alpha-casual-autoimport' and a non-empty tasks list; each entry a mapping with a non-empty unique string id; EVERY required field schema-validated (title/acceptance/verification/milestone/phase/roadmap_layer/spine_outcome/track/week must be non-empty strings - type-coercion no longer masks nonstring fields); status must be a string in the valid set (unhashable values like lists reported, not crashed on); done_sha must be an actual STRING matching 40-hex (integers are rejected, not str()-coerced into false provenance); ids must not carry surrounding whitespace; dependencies must be a list of strings, with duplicate edges explicitly rejected as 'duplicate dependency' (NOT misreported as cycles) and unknown ids flagged; cycle detection (Kahn over deduplicated edge sets, unique-id tasks only) runs only over well-formed entries. This also closes the T0020 verifier residual (dependencies:null crash, id/type strictness). Regression tests: tests/test_dag.py (16 new strictness tests incl. non-mapping board, unhashable status, duplicate-dependency-not-cycle, per-field metadata type checks + updated duplicate-id test asserting both the verify problem and the claimable/index fail-closed raise).
Commands: `python -m tools.install_checks.runner T0022 && python tools/dag.py verify`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
