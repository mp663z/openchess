# T0023 Install property check - evidence

Acceptance: property - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: tools/install_checks/check_property.py runs differential + consistency properties over tools/license_audit.expression_ok, the function that decides whether a dependency license is acceptable. Good mode: P1 differential - expression_ok must agree with an INDEPENDENT regex-fullmatch oracle (no shared code) over 5,000 seeded generated expressions from an adversarial token alphabet (allowed ids, copyleft ids, operators, case variants, junk, empty); P2 acceptance soundness - an accepted expression must contain at least one fully allowlisted OR-branch; P3 pinned corpora - benign expressions (incl. OR-with-copyleft, which SPDX OR semantics accepts) must be accepted, the 19-attack corpus must be rejected. Violation mode: the pinned attack corpus (copyleft ids, AND-mixed copyleft, parentheses, WITH exceptions, dangling/duplicated operators, empty branches, near-miss ids like MITX/MIT-0X) must ALL be rejected by the real gate. This check caught two REAL gate bugs, fixed in this PR with the oracle kept honest: doubled interior operators were salvaged ("MIT OR OR Apache-2.0" accepted) - now every OR-branch must be structurally valid (AND of bare ids, no stray operator tokens) before acceptance semantics apply; and control characters/newlines made the whitespace splits ambiguous ("MIT\nOR\nApache-2.0" accepted) - now expressions must be printable ASCII.
Commands: `python -m tools.install_checks.runner T0023`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
