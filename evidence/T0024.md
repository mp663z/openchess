# T0024 Install fuzz check - evidence

Acceptance: fuzz - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: tools/install_checks/check_fuzz.py fuzzes the fail-closed gates: license_audit.expression_ok/candidate_ok (3,000 seeded random strings from an alphabet including NULs, tabs, newlines, parens, operator fragments), rights_audit.validate_sources and cla_check.validate_registry (1,000 random YAML-shaped structures, depth-bounded). Contract: NEVER crash, NEVER hang, correct return type, and random noise must never be ACCEPTED as a license expression unless structurally allowlisted. validate_sources is called on EVERY input (no type bypass in the harness) and invalid structures must produce NONEMPTY problems (empty-accept fails). Schema-aware near-valid generators fuzz exactly one field of a valid source at a time; every field-type crash class found by structured fuzz (license/statement_source_url/evidence_path/evidence_sha256/statement non-string, decision non-string, id non-string) is seeded - and FIXED in rights_audit.validate_sources itself, which now validates strict field types before any operation. A designed domain rejection (ClaError from the registry) counts as the intended fail-closed outcome, not a crash; any other exception type fails. Violation mode: pinned nasties (10,000-token inputs, NUL runs, doubled operators, newline-separated expressions, confusable unicode, wrong-type YAML nodes, absolute/escape evidence paths, plus 10 seeded near-valid single-field mutations) must each be handled without crash AND rejected (validate_sources: nonempty problems; validate_registry: designed ClaError) - a crash or silent accept is an uncaught violation and fails the harness.
Commands: `python -m tools.install_checks.runner T0024`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
