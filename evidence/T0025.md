# T0025 Install integration check - evidence

Acceptance: integration - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: tools/install_checks/check_integration.py executes the governance CLI chain as REAL subprocesses against a REAL tracked-file copy of the repo (git ls-files -> copy). Good mode: the clean copy passes tools/license_lint.py, tools/evidence_lint.py, tools/rights_audit.py and tools/dag.py verify (all rc 0). Violation mode: five targeted tampers on fresh copies - LICENSE text altered (license_lint), tasks/dag.json duplicated task (dag verify), a recorded merge SHA forged in evidence/T0033.md (evidence_lint cross-check against board done_sha), the rights manifest truncated (rights_audit), and the import-isolation proof described below - each tampered CLI must exit with its expected nonzero outcome. Subprocess environments strip GIT_* variables AND carry no PYTHONPATH: the CLIs are self-contained (no tools.* imports), so the copy must run on its own files alone - import isolation is PROVEN by a module-tamper case (a marker + exit-3 injected into the copy's tools/rights_audit.py after its __future__ import must produce rc 3 with the marker on stdout; running the original tree's code would fail the assertion). During hook development a leaked GIT_DIR caused fixture git commands to corrupt the real repo's config, so fixture hygiene is itself part of the contract.
Commands: `python -m tools.install_checks.runner T0025`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
