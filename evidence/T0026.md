# T0026 Install clean-profile E2E - evidence

Acceptance: clean-profile E2E: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/profile_e2e.py + check_clean_profile_e2e.py. The scrubber builds a zero-user-state environment: fresh temporary HOME, only process essentials kept (PATH, SYSTEMROOT/WINDIR, TEMP/TMP, LANG/LC_ALL), everything GH_*/GITHUB_*/GIT_* dropped except the scrubber's own isolation markers (GIT_CONFIG_NOSYSTEM=1, GIT_CONFIG_GLOBAL=/dev/null), dependency-affecting and identity vars (PYTHONPATH, VIRTUAL_ENV, USER, LOGNAME, SHELL, CI) dropped and asserted absent via leaked_vars. Good mode: the scrub result provably isolates and a real gate (dag verify) passes inside it. Violation mode is canary-seeded and discriminating: each leak probe must FIRST fire against a seeded hostile profile (canary GH_TOKEN/GIT_DIR/PYTHONPATH/USER, HOME with a .gitconfig identity) - a probe that cannot fire raw fails the harness - then the same seeded profile is scrubbed and the probe must be blocked. v2 remediation: leaks are reported as ordinary failures, never raised (a raised CheckError in violation mode reads as "caught", which would reward a leaking scrubber - found by mutation testing a GH_TOKEN-keeping scrubber).

Commands: `python -m tools.install_checks.runner T0026 && python tools/profile_e2e.py`. Environment: python 3.14.
Status: done
Recorded merge SHA on main: 0ebad52d82b9dec62240546b7bbeddb07667415a
Verification: auto + fault injection - PASS at 0ebad52d82b9dec62240546b7bbeddb07667415a (independent verifier, PR head 42a720ed031dd87701d2863610aa8934b42b8d73); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
