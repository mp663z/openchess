# T0027 Install screenshots - evidence

Acceptance: screenshots: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/render_harness.py + check_screenshots.py. The harness renders a JSON spec to canonical SVG and an ASCII-PPM (P3) raster - same spec, same bytes, no clocks, randomness or environment bleed (double-render determinism asserted). Goldens are tracked under tools/install_checks/fixtures/screens/; good mode byte-compares svg and ppm renders against them and fails on missing goldens. Violation mode: a tampered spec (accent + grid color) must NOT match the good golden in either format, with the mismatch quantified (pixel count); the control render must still match. Structural pixel differences (header/geometry) always count as a difference, never as "no diff" (aligned with the T0028 v3 strict parser).

Commands: `python -m tools.install_checks.runner T0027 && python tools/render_harness.py svg tools/install_checks/fixtures/screens/good_card.json`. Environment: python 3.14.
Status: done
Recorded merge SHA on main: d60abc4c8ef620124df7b1ce4ca9e711d6527a02
Verification: auto + fault injection - PASS at d60abc4c8ef620124df7b1ce4ca9e711d6527a02 (independent verifier, PR head 06ba3793cbc3f5e61a4b3f1fe25e226653839fed); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
