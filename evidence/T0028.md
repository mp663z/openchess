# T0028 Install pixel/a11y - evidence

Acceptance: pixel/a11y: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW check_pixel_a11y.py over tools/render_harness.py. Pixel: strict P3 parsing (magic, positive dimensions, maxval range, exactly w*h*3 channels, values in range - violations raise ValueError) and pixel_diff returns -1 on any header mismatch, so geometry-only (34x34 to 17x68, same payload) and maxval-only (255 to 1) changes are structural differences, never zero (v3 remediation after the verifier showed both bypasses returning zero diff); a single flipped pixel is caught with an exact diff count of 1. a11y: spec rules - non-empty title and row labels, WCAG text/background contrast >= 4.5 via relative luminance, no color-only emphasis (a toned row must carry a text marker); one violation fixture per rule, each caught by its intended rule. Non-blocking scope note (verifier): the color-only rule checks tone-declaring rows, not a general color analysis.

Commands: `python -m tools.install_checks.runner T0028`. Environment: python 3.14.
Status: done
Recorded merge SHA on main: d60abc4c8ef620124df7b1ce4ca9e711d6527a02
Verification: auto + fault injection - PASS at d60abc4c8ef620124df7b1ce4ca9e711d6527a02 (independent verifier, PR head 06ba3793cbc3f5e61a4b3f1fe25e226653839fed); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
