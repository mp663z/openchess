# T0030 Install fault/crash - evidence

Acceptance: fault/crash: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/fault_inject.py + check_fault_crash.py. Outcome classifier: pass (exit 0), reject (exit 1 with named stderr), crash (timeout kill, signal death, exit > 1, or silent exit 1 - an undiagnosable failure is a crash). Faults: no-home, readonly-home, SIGTERM kill. Kill path: polls after a 300ms grace and classifies the actual exit if the process already finished; once the pre-signal poll proves live and the signal is delivered, ANY post-signal exit classifies as crash (negative = terminated, 0/1 = handled/swallowed SIGTERM - a masked kill is never a pass, v3 remediation after the verifier reproduced a SIGTERM-handler exit-0 reading green). Good mode: a real gate (dag verify) passes normally and never crashes under faults; kill classification proven on a 30s sleep (crash), immediate-pass (pass), immediate-reject (reject) and sigterm_swallow (crash) fixtures. Violation mode: hang killed at timeout, silent-accept flagged when reject expected, exit-3 classified crash. Combined mutation (no poll + unconditional crash) fails good mode; each defense layer alone holds.

Commands: `python -m tools.install_checks.runner T0030 && python tools/fault_inject.py`. Environment: python 3.14.
Status: done
Recorded merge SHA on main: 0ebad52d82b9dec62240546b7bbeddb07667415a
Verification: auto + fault injection - PASS at 0ebad52d82b9dec62240546b7bbeddb07667415a (independent verifier, PR head 42a720ed031dd87701d2863610aa8934b42b8d73); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
