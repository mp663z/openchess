# T0031 Install secrets - evidence

Acceptance: secrets: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/secrets_scan.py + check_secrets.py. Five high-signal pattern classes over all tracked files (private-key headers, GitHub token prefixes, AWS AKIA/ASIA access keys, Slack xox* tokens, secret-looking assignments); no entropy guessing. Fail closed on unreadable or oversized files (25MB cap covers tasks/dag.json); GIT_* stripped from the ls-files subprocess so a hook context cannot redirect the listing; the install-checks fixtures directory is excluded from the tree scan and scanned directly in violation mode. Good mode: real tree scans clean. Violation mode: one planted fixture per class, all five detector classes must fire (detector coverage asserted - a neutered pattern fails the harness), and the clean fixture must stay clean. GitHub push protection rejected the first Slack fixture shape; reshaped to satisfy the scanner without tripping push protection. Verifier: PASS v1 with a non-blocking scope caveat.

Commands: `python -m tools.install_checks.runner T0031 && python tools/secrets_scan.py`. Environment: python 3.14.
Status: done
Recorded merge SHA on main: 7850635777231f05bf34284dbb42bb217d81291c
Verification: auto + fault injection - PASS at 7850635777231f05bf34284dbb42bb217d81291c (independent verifier, PR head 1c59bacd191b48c618774266ce0078d733142576); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
