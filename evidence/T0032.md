# T0032 Install dependency license check - evidence

Acceptance: dependency license - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: check_dependency_license.py wraps the real tools/license_audit.py. Good mode: audit over the repo's installed requirements is clean. Violation mode: seeded fixtures/seedgpl-1.0.dist-info (License: GPL-2.0-only) audited in a FRESH subprocess with the fixtures dir on sys.path (importlib.metadata caches make in-process injection unreliable - verified empirically); the audit must reject it.
Tests: as T0021, in tests/test_install_checks.py.
Commands: `python -m tools.install_checks.runner T0032`. Environment: python 3.12.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
