# T0033 Install data/model rights check - evidence

Acceptance: data/model rights - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/rights_audit.py is the fail-closed validator (single source of truth; tests/test_public_source_rights.py and tests/test_rights_manifest_hashes.py delegate to it). Beyond field presence, every claim is checked against something independent: https-only URLs with real hosts; statement_source_url is https or an existing repo-local path CONTAINING the cited statement verbatim; decision=allow requires a policy-set license (CC0/permissive) or the pinned user-owned-files exemption, plus an explicit transformation grant; evidence is pinned BYTES - data/datasets/statements/<id>.txt snapshots with fetched_from/fetched_at provenance, evidence_sha256 recomputing over the snapshot bytes, statement verbatim inside; fetched_at validated as a real non-future ISO date parsed from the ENTIRE value (trailing garbage like '2026-09-18 NOT-ISO' is rejected, not truncated); evidence/local paths constrained inside their base (no absolute/.. escapes); evidence must live under data/datasets/statements/ in the full audit, enforced on RESOLVED paths ((base/ev).resolve().is_relative_to the resolved statements dir) - lexical prefix attacks such as data/datasets/statements/../../../elsewhere.txt that resolve inside the repo are rejected. The manifest (data/datasets/public-source-rights.yaml, 11 sources) was migrated from self-referential statement_sha256 to evidence_path + evidence_sha256. Check: check_data_model_rights.py - good mode runs the full audit on the real manifest; violation mode runs 8 seeded fixtures (allow without grant, tampered evidence hash, unknown decision, missing license, fabricated license string, javascript: URL, self-authored grant absent from its evidence bytes, missing local source path, malformed fetched_at, future fetched_at, evidence path escape), each must be rejected for its own reason; v4 adds the verifier's exact attacks as permanent seeds: trailing-garbage fetched_at (bad_fetched_at_trailing.yaml) and a statements-dir escape resolving inside the base (bad_dir_escape.yaml, caught by the full audit's resolved confinement). HONEST SCOPE (pinned by test_fabricated_snapshot_residual_is_human_gated): a fabricated snapshot consistent with its own manifest entry passes the mechanical gate - the gate proves bytes/consistency, not remote truth; that class is human-gated because snapshots live under the CLA-protected data/ prefix (non-de-minimis review), with re-fetch verification at acquisition time. Verifier's v1 attack (fake license + javascript: URL + self-consistent self-authored grant) is a permanent test: tests/test_public_source_rights.py::test_verifier_attack_fabricated_grant_is_rejected. Coverage mutation test: removing required-source coverage breaks the full audit.
Commands: `python -m tools.install_checks.runner T0033 && python tools/rights_audit.py`. Environment: python 3.12.
Status: done
Recorded merge SHA on main: fd5e87e807777775668cebe5a11019298df9c309
Verification: auto + fault injection - PASS at fd5e87e807777775668cebe5a11019298df9c309 (independent verifier, PR head a78260ffeff263e8382aaa2a4f540ba2c1376448)
