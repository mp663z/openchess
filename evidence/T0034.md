# T0034 Install SBOM/signing check - evidence

Acceptance: SBOM/signing - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: check_sbom_signing.py. SBOM: committed docs/component-inventory.json must regenerate EXACTLY via tools/component_inventory.py; fault injection - a library dropped from the real linkage classification fails generation naming it, a tampered manifest mismatches regeneration. Signing: release.yml now performs REAL keyless Sigstore attestation (was: a job merely NAMED attest) - OIDC permissions (id-token: write, attestations: write) + actions/attest-build-provenance@v2 with subject-path covering the SBOM, the release lock and checksums.sha256, all three also attached to the release. The check PARSES release.yml as YAML and enforces job-level structure: it locates jobs with an EXECUTABLE gh release create step (a run line starting with the command token - echo/comment mentions do not count), fails closed on ANY job-level if on the release job and ANY if on a mandatory generation/checksum/attestation/release step (GitHub expression syntax like ${{ false }} is not parsed - conditionals on mandatory work are simply forbidden), requires the release step to be a SINGLE command line (multi-line shell such as an 'exit 0' prefix before gh release create is a bypass and is rejected), checks EFFECTIVE permissions (job overrides workflow) for id-token+attestations write, and requires generation -> checksums -> attestation -> release steps IN THAT JOB in that order (cross-job masking impossible). Ten seeded mutations each caught: attestation removed, SBOM dropped from subjects, OIDC downgraded, artifacts detached, job disabled (if:false and if:${{ false }}), attestation step gated (if:${{ false }}), release command echoed-not-executed, release command after exit 0, attestation split into an unrelated job.
Commands: `python -m tools.install_checks.runner T0034`. Environment: python 3.12.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
