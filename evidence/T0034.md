# T0034 Install SBOM/signing check - evidence

Acceptance: SBOM/signing - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: check_sbom_signing.py. SBOM: committed docs/component-inventory.json must regenerate EXACTLY via tools/component_inventory.py; fault injection - a library dropped from the real linkage classification fails generation naming it, a tampered manifest mismatches regeneration. Signing: release.yml now performs REAL keyless Sigstore attestation (was: a job merely NAMED attest) - OIDC permissions (id-token: write, attestations: write) + actions/attest-build-provenance@v2 with subject-path covering the SBOM, the release lock and checksums.sha256, all three also attached to the release. The check PARSES release.yml as YAML and enforces job-level structure: it locates jobs with an EXECUTABLE gh release create step (a run line starting with the command token - echo/comment mentions do not count), fails closed on ANY job-level if on the release job and ANY if on a mandatory generation/checksum/attestation/release step (GitHub expression syntax like ${{ false }} is not parsed - conditionals on mandatory work are simply forbidden), requires the release step to be a SINGLE command line (multi-line shell such as an 'exit 0' prefix before gh release create is a bypass and is rejected), checks EFFECTIVE permissions (job overrides workflow) for id-token+attestations write, and requires generation -> checksums -> attestation -> release steps IN THAT JOB in that order (cross-job masking impossible). Mandatory steps are pinned by EXACT structural equality (whitespace-normalized): generation run `python tools/component_inventory.py`, checksums run `sha256sum docs/component-inventory.json data/release-lock.json > checksums.sha256`, attestation uses `actions/attest-build-provenance@v2`, release run the exact gh release create command - echo prefixes, lookalike actions (attest-not-real) and lookalike commands (createx) all fail closed. The COMPLETE release-job pipeline is pinned step-for-step (identity + order, 9 steps from checkout to release): any inserted, removed, reordered or altered step fails closed, so attested bytes and released bytes cannot drift apart; conditionals are rejected on EVERY pipeline step, not only the four mandatory ones. Step metadata is fail-closed too: only uses/run/name/with/env are allowed on pipeline steps (continue-on-error, working-directory, shell and any other execution-affecting key is a violation), setup-python's with must be exactly python-version 3.12, the attestation with must be only subject-path, and the release env must be exactly GH_TOKEN from secrets.GITHUB_TOKEN. Job-level metadata is fail-closed with the same allowlist approach: only name/runs-on/steps/permissions are permitted on the release job (runs-on pinned to exactly ubuntu-latest) - defaults (e.g. run.shell), job env, container, services, strategy, continue-on-error and any other unreviewed key is a violation. ENDGAME (v9): the ENTIRE release.yml effective structure is pinned as one canonical object - trigger, global permissions, job keys, the full ordered step list, and exact with/env contents at every level. Any deviation anywhere (any scope level: workflow, job, step) fails closed with the first differing paths named; no unchecked scope level remains. Twenty-four seeded mutations each caught: attestation removed, SBOM dropped from subjects, OIDC downgraded, artifacts detached, job disabled (if:false and if:${{ false }}), attestation step gated (if:${{ false }}), release command echoed-not-executed, release command after exit 0, attestation split into an unrelated job, generation echoed, checksums echoed, lookalike attestation action, lookalike release command, tamper inserted between checksums and attestation, tamper inserted between attestation and release (released bytes no longer the attested bytes), attestation continue-on-error:true (unattested bytes would still publish), release under shell 'echo {0}' (rendered, not executed), generation in working-directory /tmp, job defaults.run.shell 'echo {0}' (every run step rendered, not executed), job env PATH /tmp (command resolution pivoted), job container alpine:latest (execution environment pivoted), top-level defaults.run.shell 'echo {0}', top-level env PATH /tmp.
Commands: `python -m tools.install_checks.runner T0034`. Environment: python 3.12.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
