# T0036 Install protected branch - evidence

Acceptance: protected branch: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/branch_guard.py reconciles the local protection machinery against data/branch-protection.yaml (strict schema: protected_ref + required_checks, every entry naming a real "<workflow> / <job>"). core.hooksPath must be exactly .githooks; .githooks/pre-push must exist, be executable and run the pinned gate commands as full lines in exact relative order; the CI workflow must carry the pinned step names as full step names in exact relative order (subsequence, never substring). The documented fresh-clone install step is tools/setup.sh carrying exactly `git config core.hooksPath .githooks`, pinned by the guard and wired into ci.yml, canary.yml and the clean verifier's clone path - surfaced as a real defect by the clean verifier (hooksPath is local config, invisible in a fresh clone) and fixed here. The remote GitHub branch-protection settings cannot be verified hermetically; they are fetched once for this evidence via gh api and are human-review scope, no mechanical claim. v2 remediation (FAIL at c1503b13): subsequence/name-only pinning replaced with whole-structure pins - the pre-push hook must equal the pinned script exactly (shebang + every non-comment, non-blank line; a prepended `exit 0`, reorder, removal or any extra executable line fails) and ci.yml must equal the pinned parsed structure exactly (step names AND commands, action versions, the CLA gate's condition and env, via a recursive first-differ; an echo-swapped run, added `if`, continue-on-error, custom shell or any other drift fails). Editing the hook or CI deliberately requires updating the pin in the same change. Check: check_branch_protection.py - good mode verifies the real repo; v3 remediation (FAIL at 2470a839): tools/setup.sh is now pinned the same way as pre-push - whole effective script (shebang + every non-comment, non-blank line), so an early exit or inert echo before the config line fails. violation mode seeds fourteen fixture trees (hook missing a gate, not executable, reordered, early-exit prepended, inert CI echo step, CI conditional, continue-on-error, custom shell, policy naming a nonexistent job, wrong-type protected_ref, setup.sh missing, setup.sh missing the pinned line), each caught for its own reason.

Commands: `python -m tools.install_checks.runner T0036 && python tools/branch_guard.py`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
