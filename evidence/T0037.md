# T0037 Install flake quarantine - evidence

Acceptance: flake quarantine: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW data/flake-quarantine.yaml (currently zero entries) + tools/flake_quarantine.py. Schema is exact-key ({test_id, reason, added, expires}); every entry must carry a non-empty reason, ISO dates, an unexpired expiry, a unique test_id that exists in the collected pytest suite (pytest --collect-only -q), and the gate set (collected minus quarantined) must be non-empty. A stale or fabricated quarantine entry is a gate failure, not drift. v2 remediation (FAIL at c1503b13): date semantics completed - added must be <= today (no future-dated entries) and expires must be >= added; pytest collection now fails closed (nonzero exit raises with stderr, converted to a gate problem) instead of silently yielding an empty collected set. Check: check_flake_quarantine.py - good mode validates the real repo (0 quarantined, 376 tests in the gate set); violation mode seeds unknown test id, expired entry, future-added entry, expires-before-added, blank reason, duplicate id, bad date, extra key, non-mapping entry, empty-gate-set and a real collection failure, v3 remediation (FAIL at 2470a839): check good mode converts a pytest collection failure into a named T0037 CheckError instead of dying with a traceback; added/expires types are validated strictly (datetime and integer values rejected, never coerced via str). Seeds added: integer date, datetime value.

Commands: `python -m tools.install_checks.runner T0037 && python tools/flake_quarantine.py`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
