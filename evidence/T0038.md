# T0038 Install clean verifier - evidence

Acceptance: clean verifier: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/clean_verify.py. HEAD is cloned into a fresh directory (committed content only - untracked and ignored files never cross; full history so git-dependent gates run identically), the documented setup (tools/setup.sh) runs there, and the nested install-check runner re-runs with an allowlist-scrubbed environment (PATH/HOME/LANG/locale/TMP only - GIT_*, credentials, CI flags, venv vars, PYTHONPATH never cross). A clean-copy failure or any in-tree vs clean-copy verdict divergence is a gate failure: a pass that depends on leaked environment or untracked state is not a pass. Recursion guard: runner.nested_cmd() passes --exclude T0038,T0039 so checks that shell the runner never recurse; the exclusion travels as a flag so the nested runner discovers its own committed check set. Building this surfaced a REAL defect: core.hooksPath is local config and does not exist in a fresh clone - fixed by T0036's tools/setup.sh. v2 remediation (FAIL at c1503b13): bounded subprocess timeouts (600s default; a hung gate is a failed gate, exit 124, in-tree and clean copy) and the clone's origin/main is now pinned to the source's origin/main sha read before cloning (update-ref + readback, fail closed on drift) - the history anchor cannot move mid-verification. Check: check_clean_verifier.py - good mode re-verifies the real repo; violation mode seeds a poisoned environment (GIT_DIR/GH_TOKEN/PYTHONPATH/CI/VIRTUAL_ENV/arbitrary - scrubbed to the allowlist), a hung gate (timeout = failure), a fixture repo whose gate passes only with a leaked env var, and a $HOME-dependent gate (v3: the clean copy gets a fresh temp HOME so HOME-resident state cannot pass both sides and evade divergence detection). v3 also bounds the setup.sh subprocess (60s, TimeoutExpired = setup failure).

Commands: `python -m tools.install_checks.runner T0038 && python tools/clean_verify.py`. Environment: python 3.14.
Status: done
Recorded merge SHA on main: 49371af639786f915bb9ae234d4b58f4b182c7b8
Verification: auto + fault injection - PASS at 49371af639786f915bb9ae234d4b58f4b182c7b8 (independent verifier, PR head 6f83d23f653ff2ac5e62e57bfde17df79d2e8d65); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
