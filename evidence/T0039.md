# T0039 Install post-merge canary - evidence

Acceptance: post-merge canary: seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/canary.py + .github/workflows/canary.yml. After every merge to main the canary re-runs the merge-relevant gate set (DAG verify, DAG reconcile, evidence contract, license audit, license lint, governance lint, and the nested install-check runner) against the checked-out HEAD; every failure names the head SHA. The workflow pin is itself gated: on.push.branches must be ["main"] exactly (PyYAML 1.1 bare-`on` quirk handled) and the canary job must run tools/canary.py after tools/setup.sh. Recursion guard: canary gates use runner.nested_cmd() (--exclude T0038,T0039); check_post_merge_canary.py good mode validates the workflow pin, runs the non-runner gates against the real repo, and asserts the nested-runner gate is present structurally (the enclosing runner already covers it). Violation mode seeds a widened trigger, a non-main trigger, a missing canary step, a missing canary job, and a failing gate against a fixture repo HEAD (must be reported naming the head SHA), each caught for its own reason.

Commands: `python -m tools.install_checks.runner T0039 && python tools/canary.py`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
