# T0040 Install DAG reconcile - evidence

Acceptance: DAG reconcile - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/dag_reconcile.py reconciles the three sources of truth about done-ness: tasks/dag.json (status=done + done_sha), evidence/T*.md (Status: done + Recorded merge SHA on main), and git history (every done_sha must be reachable from origin/main - an unreachable SHA is fabricated provenance). BOTH directions are checked: board-done implies evidence-done with the same SHA; evidence-done implies board-done; evidence for an absent task is flagged. Pre-contract grandfathered evidence (data/evidence-pre-contract.yaml allowlist + pinned sha256 bytes + the closing marker, marker regex imported from tools.evidence_lint - single source) is exempt from ONLY the evidence-format comparison; done_sha validity (exact 40-hex string, never type-coerced) and history reachability run ALWAYS, grandfathered or not. Board-done requires its evidence file to exist (missing file flagged, not skipped). The cross-source pointer is enforced by reconcile itself for every board-done task: evidence_manifest must be exactly evidence/<id>.md, grandfathered or not. Global board structure (schema version, duplicate ids, malformed entries, invalid status, cycles, done-requirements) is rejected by delegating to the real tools.dag.verify - same engine as the board tooling, no second implementation. Building the tool surfaced two REAL provenance defects, both repaired on main before this check existed: T0014's done_sha pointed at an orphaned first merge attempt (a2c6fb7, never reached main; corrected to 98c8b368, independently confirmed by the verifier), and ten board-done tasks (T0010-T0013, T0015-T0018, T0020, T3761) carried stale in-progress evidence - completed with verbatim SHAs and PR heads extracted from git history. Check: check_dag_reconcile.py - good mode reconciles the real repo (54 done tasks agree); violation mode seeds twelve synthetic mismatches (board done/evidence not done, SHA mismatch, orphan SHA, evidence done/board not done, evidence for absent task, grandfathered-with-orphan-SHA, missing evidence file, missing done_sha, integer done_sha, duplicate board ids, wrong evidence_manifest pointer, malformed status), each caught for its own reason.
Commands: `python -m tools.install_checks.runner T0040 && python tools/dag_reconcile.py`. Environment: python 3.14.
Status: done
Recorded merge SHA on main: d0a2ef7c4f3eb2414acff90b8dbf528c3c8a2a23
Verification: auto + fault injection - PASS at d0a2ef7c4f3eb2414acff90b8dbf528c3c8a2a23 (independent verifier, PR head f1413dbc2b58262e7b2c6bf47fb9136a7cfa1f21); owner judgment delegation for this governance chain confirmed over the user's own WhatsApp channel (04:28 IST grant)
