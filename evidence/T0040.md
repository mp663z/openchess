# T0040 Install DAG reconcile - evidence

Acceptance: DAG reconcile - seeded good case passes and seeded violation fails, naming task and SHA.

Implementation: NEW tools/dag_reconcile.py reconciles the three sources of truth about done-ness: tasks/dag.json (status=done + done_sha), evidence/T*.md (Status: done + Recorded merge SHA on main), and git history (every done_sha must be reachable from origin/main - an unreachable SHA is fabricated provenance). BOTH directions are checked: board-done implies evidence-done with the same SHA; evidence-done implies board-done; evidence for an absent task is flagged. Pre-contract grandfathered evidence (data/evidence-pre-contract.yaml allowlist + pinned sha256 bytes + the closing marker, marker regex imported from tools.evidence_lint - single source) is exempt from the done-format but NEVER from the history anchor. Building the tool surfaced two REAL provenance defects, both repaired on main before this check existed: T0014's done_sha pointed at an orphaned first merge attempt (a2c6fb7, never reached main; corrected to 98c8b368, independently confirmed by the verifier), and ten board-done tasks (T0010-T0013, T0015-T0018, T0020, T3761) carried stale in-progress evidence - completed with verbatim SHAs and PR heads extracted from git history. Check: check_dag_reconcile.py - good mode reconciles the real repo (54 done tasks agree); violation mode seeds five synthetic mismatches (board done/evidence not done, SHA mismatch, orphan SHA, evidence done/board not done, evidence for absent task), each caught for its own reason.
Commands: `python -m tools.install_checks.runner T0040 && python tools/dag_reconcile.py`. Environment: python 3.14.
Status: in-progress
Verification: auto + fault injection - routed to the verifier with the PR head SHA
