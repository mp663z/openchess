# T0042 Chess/variant/fixture - evidence

Fixture: tests/fixtures/variant/cases.json (schema variant-fixture-1) -
conformance cases for the T0041 variant + position-identity contract:
3 happy (exact canonical identity tuples), 6 boundary (structured
{variant, fen} records; one-field canonical deltas via declared
changed_fields; each canonical dimension covered exactly once; the
variant dimension is an explicit deferred marker with a structured
contract-path hook while the registry holds one variant), 14 malformed
(one per declared failure class, each failing at exactly its own stage
with pairwise-disjoint markers), 2 executable rollback cases (unknown
variant fails closed through the real registry gate with a legal FEN
payload; additive fields tolerated under canonical projection).
Enforcement: tests/test_t0042_fixture.py - 14 tests: strict shapes
(unknown keys / typo'd expectations / duplicate names are violations),
load-bearing expectations (observed error code == expect_failure ==
expect_error_code == contract-pinned malformed_request), per-case
discriminating mutation batteries (wrong failure class per case,
sibling error codes per rollback case, canonical-field clobbering).
tools/variant_contract_lint.py gains check_variant_id (fail-closed
registry gate, malformed_request prefix).
Chain: PR #78, verifier machine-prep PASS at
654279d0d4346f5afbe6346e9675fe474f9f3fba (v4); merged as
87034459a8ff65f8a403be66852a89aa7db99a3e. Governance authority: owner
wamid.HBgMOTE4MTIxNzk4Mjg1FQIAEhgUM0IxQzU0M0UzNDg2NDU1NDdERDUA
(2026-09-19, this governance chain only).

Status: done
Verification: auto - contract lint + 14-test fixture battery green on main
Commands: `python tools/variant_contract_lint.py && python -m pytest tests/test_t0042_fixture.py -q`. Environment: python 3.12 sandbox, PyYAML; main at merge 8703445.
Recorded merge SHA on main: 87034459a8ff65f8a403be66852a89aa7db99a3e
