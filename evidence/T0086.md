# T0086 Chess/FEN contract - evidence

data/contracts/fen.yaml + tools/fen_contract_lint.py +
tests/test_t0086_fen_contract.py (81-case battery). The contract pins
FEN parse/emit in exact-pinned structured fields: six-field grammar,
placement rules, position_rules (exactly one king per side, kings not
adjacent, no back-rank pawns, non-mover king not attacked, material
bounds pawns<=8/pieces<=16 with promotion budget), ASCII-only counters
with leading zeros forbidden, castling consistency against the LINKED
castling contract home_squares, en-passant target/origin/halfmove
consistency against the LINKED en-passant set_on and turn contract
clock reset, exact-input-string serialization. The reference model is
fully contract-derived (board geometry, attack vectors, castling
homes, EP geometry). v3: pawn attack relation uses inverse-origin
lookup with 8 adjacent direction witnesses, wrap guards, and a
delta-sign-flip mutation replay. Chain: PR #119, verifier #2 PASS at
bed6e83282b0f319f749e8647a08659568aa740d (v3, 3 rounds).

Status: done
Verification: auto - independent verifier #2 PASS at bed6e83282b0f319f749e8647a08659568aa740d (PR #119); battery green: 81 passed, full repo suite green via pre-push hook
Commands: `.venv/bin/python -m pytest tests/test_t0086_fen_contract.py -q`; `.venv/bin/ruff check .`. Environment: python 3.10.12 sandbox venv, pytest, PyYAML, ruff, zstandard; main merge commit e6d3ad0.
Recorded merge SHA on main: bed6e83282b0f319f749e8647a08659568aa740d
