# T0087 FEN parser fixture - evidence

tests/fixtures/fen/cases.json (schema 3) + tests/test_t0087_fen_fixture.py
(89-test battery over 29 cases: happy / boundary / malformed /
rollback). The fixture executes against the merged T0086 FEN
contract-derived reference (parse_fen, emit_fen, FenError with pinned
failure_class subclasses). Malformed cases are single-defect
SEMANTICALLY: every non-target rule passes in the input, the
declarative minimal_repair (find/replace span, sole repair source
pinned by fixture schema) removes only the targeted violation, and
every parametrization is derived from the fixture itself with a
meta-test pinning test_malformed parameter IDs to the fixture
malformed-name set. Layers classify each defect to the contract
validation layer that must catch it; an in-memory injected valid
"malformed" row proves the rejection test is not vacuous; the pinned
FIXTURE_SCHEMA_VERSION = 3 closes the fixture format (2, 4, "3",
True, and missing schema all fail structure validation). DESIGN
CAUTION recorded: the fixture proves fixture/contract consistency,
not production behavior.

Verifier #1 PASS at 6ddf475859a0834cb4a99ebe9696394cec4917d9 (v4,
three remediation rounds: single-defect redesign with minimal
repairs; fixture-derived parametrization + minimal_repair single
source; fixture-version closure).
Recorded merge SHA on main: 6ddf475859a0834cb4a99ebe9696394cec4917d9
(merge commit 06fe1223697dbc843f4f3b2eb9cd6730e42831fd).

Status: done
Verification: auto - independent verifier #1 PASS at 6ddf475859a0834cb4a99ebe9696394cec4917d9 (PR #126); battery green: 89 passed, full repo suite 2066 passed / 5 skipped
Commands: `.venv/bin/python -m pytest tests/test_t0087_fen_fixture.py -q` (89/89); `.venv/bin/python -m pytest tests/ -q` (2066 passed, 5 skipped); `.venv/bin/ruff check tests/test_t0087_fen_fixture.py` clean. Environment: python 3.10.12 sandbox venv, pytest, PyYAML, ruff, zstandard; branch feat/t0087-fen-fixture off main e604c04; main merge commit 06fe122.
