# T0113 Chess/position digest contract - evidence

data/contracts/position_digest.yaml + tools/position_digest_contract_lint.py
+ tests/test_t0113_position_digest_contract.py (52-test battery). The
contract pins the position digest in exact-pinned structured fields:
accelerator-only role per the linked variant contract's hash_rule
(equality is always canonical field comparison), the identity tuple
sourced from the variant contract's canonical_fields (never restated),
the en-passant component as the linked en-passant contract's identity
value (target only when a legal capture exists, verified through the
FEN attack relation incl. the rank-pin case), the canonical byte
encoding (variant registry id - every id provably delimiter-safe under
the NEW variants.id_grammar pinned in variant.yaml and enforced by both
linters - FEN-canonical placement, active color, castling, ep identity
value; ASCII only, exactly four single-space separators), full
untruncated SHA-256 as exact pdv1:+64 lowercase hex with prefix-bump
versioning, three failure classes onto the closed enum, and
collision-aware properties: same-identity-same-digest is the only
deterministic guarantee; field changes provably change the ENCODING,
digest changes are vector-pinned, unequal tuples may collide and fall
back to field comparison. Chain: PR #123, verifier #1 PASS at
1750d32ac3d14de0a40703aa79006a58c09a1f95 (v2, 2 rounds).

Status: done
Verification: auto - independent verifier #1 PASS at 1750d32ac3d14de0a40703aa79006a58c09a1f95 (PR #123); battery green: 52 passed, variant battery 116 passed, full repo suite 1971 passed / 5 skipped
Commands: `.venv/bin/python -m pytest tests/test_t0113_position_digest_contract.py -q`; `.venv/bin/ruff check .`. Environment: python 3.10.12 sandbox venv, pytest, PyYAML, ruff, zstandard; main merge commit 0ce04e5.
Recorded merge SHA on main: 1750d32ac3d14de0a40703aa79006a58c09a1f95
