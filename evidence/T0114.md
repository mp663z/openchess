# T0114 Chess/position digest/fixture - evidence

tests/fixtures/position_digest/cases.json +
tests/test_t0114_fixture.py (6-test battery over 19 cases: 4 happy /
6 boundary / 7 malformed / 2 rollback). The fixture executes against
the merged T0113 contract-derived reference (digest_fen, parse_digest)
and the T0086 FEN contract (parse_fen, FenError) for pinned failure
subclasses; digests pinned from that reference at authoring time.
Happy: startpos, phantom-target, capturable target, text roundtrip.
Boundary: kings-only, clock invariance, phantom-target collapse
(no-adjacent-pawn and rank pin), capturable-target sensitivity.
Malformed: all discriminating - repairs are DECLARATIVE data
(find/replace span or named field + replacement) and one generic test
proves the original rejects with the pinned class, the repair touches
ONLY the declared defect location (other five FEN fields / text
outside the span byte-identical), and the repaired input succeeds.
Rollback: rejected parse and rejected position leave no trace
(pure-function surface). DESIGN CAUTION recorded: the fixture proves
fixture/contract consistency, not production behavior.

Verifier #1 PASS at e8c30aa9537801b1047f3895a38607d25df30537 (v2, one
remediation round: multi-defect grammar case replaced with
single-defect `garbage w - - 0 1`, repairs made declarative).
Recorded merge SHA on main: e8c30aa9537801b1047f3895a38607d25df30537
(merge commit f07b9749cffdb9a8fe06180d77f8eb5c75da3d34).

Commands: `.venv/bin/python -m pytest tests/test_t0114_fixture.py -q` (6/6); `.venv/bin/python -m pytest tests/ -q` (1977 passed, 5 skipped); `.venv/bin/ruff check .` clean. Environment: python 3.10.12 sandbox venv, pytest, PyYAML, ruff, zstandard; branch feat/t0114-position-digest-fixture off main 77c5ce0; main merge commit f07b974.
