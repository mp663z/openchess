# T0123 transposition-node fixture - evidence

tests/fixtures/transposition_node/cases.json (fixture schema v2) +
tests/test_t0123_fixture.py (18-test battery). The fixture proves the
T0122 transposition-node contract behavior over pinned real positions:
startpos, kings-only, phantom vs real en-passant collapse, and a
two-path transposition pair, each with digest values traced through the
reference implementation before pinning. Malformed cases are
single-defect SEMANTICALLY (the two-white-kings case keeps exactly one
black king), and declarative minimal_repair is an EXACT closed tagged
union validated structurally pre-execution: span form exactly
{find, replace} (strings, find nonempty and unique, node-insert
input_fen only), set form exactly {set} (nonempty mapping,
kind-restricted keys, exact value types), remove form exactly {remove}
(nonempty duplicate-free string keys, record-validate only, each key
present in the record), mixed forms forbidden. 18 kind-targeted schema
mutations all fail _validate_structure before execution; the fixture
schema version is pinned at 2 with updated version mutations; every
parametrize list is fixture-derived with a collection meta-test
inspecting the actual pytestmark values plus a late-row witness.

Verifier #1 PASS at e8638b4e8bbf01a74b83d806b6a8df6d63013b64 (v3,
three remediation rounds: single-defect king case + collection
meta-test; exact minimal_repair tagged union + schema v2).
Recorded merge SHA on main: e8638b4e8bbf01a74b83d806b6a8df6d63013b64
(merge commit ea25b74e3c226ea32a6ea95467f316b6cae987dc).

Status: done
Verification: auto - independent verifier PASS at e8638b4e8bbf01a74b83d806b6a8df6d63013b64 (PR #127); battery green and full repo suite green at merge time
Commands: `.venv/bin/python -m pytest tests/test_t0123_transposition_fixture.py -q`; `.venv/bin/python -m pytest tests/ -q`. Environment: python 3.10 sandbox venv, pytest, PyYAML, ruff, zstandard; branch feat/t0123-transposition-fixture; main merge commit ea25b74.
