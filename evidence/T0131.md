# T0131 chess route-edge contract - evidence

data/contracts/route_edge.yaml (schema v1) + tools/route_edge_contract_lint.py +
tests/test_t0131_route_edge_contract.py (36-test battery) +
docs/t0131-route-edge-design.md. The contract is the source of truth for the
directed graph edge between two transposition nodes over one move: canonical
four-tuple identity (variant, move, from_node, to_node) sourced from the linked
T0122 node contract's identity (exact same exclusion list, accelerator-only
bucket role), move form read from the linked legal-moves move_model with
application delegated and determinism pinned (one from-identity plus one move
never reaches two target identities), exact four-field record (variant
registry-id, move, from/to canonical snapshot FENs validated through the node
contract's own record validator, no stored derived fields), ATOMIC merge
consuming exact validated source records (staged preflight, conflicting tables
reject in both orders, no partial commit, no laundering through
reconstruction), four closed failure classes with exact mapping, and linkage
verified against the actual variant/FEN/digest/node/legal-moves artifacts. 35
contract mutants over all 10 sections; digest-collision separation under an
injected constant oracle.

Verifier #2 PASS at 919e202c3da4e5f313f9d64bb7e8e31e15033b8b (v3, three
remediation rounds: atomic merge + batch-conflict pins; both-orders-reject and
batch rollback; merge validates exact source records before staging).
Recorded merge SHA on main: 919e202c3da4e5f313f9d64bb7e8e31e15033b8b
(merge commit 9b59bec).
