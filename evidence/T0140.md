# T0140 chess opening-context contract - evidence

data/contracts/opening_context.yaml (schema v1) + data/openings/registry.yaml
(seed registry, 7 ECO entries) + tools/opening_context_contract_lint.py +
tests/test_t0140_opening_context_contract.py (36-test battery) +
docs/t0140-opening-context-design.md. The contract is the source of truth for
path-attributed opening classification - the ONE graph attribution keyed by
(variant, ordered move path), never by canonical node identity, per the product
model's position-identity / move-order-path / repertoire-context separation.
Resolution: longest registry prefix wins, none sentinel for both code and name
when unclassified, empty path always unclassified, determinism and
extension_refinement pinned (an extension resolves to the same entry or a
strictly longer extending entry), shorter_key_immutability pinned. Registry
invariants lint-enforced against the actual data (ECO code grammar, unique
codes, unique NONEMPTY move sequences, move grammar from the linked legal-moves
move_model). ATOMIC merge consumes exact stored records: existing-key context
mismatch raises conflicting_context before mutation, unknown code under the
receiver registry raises unknown_opening_code separately, batch conflicts
reject wholesale. The headline witness is end-to-end: both headline
transposition paths applied move-by-move from the variant contract's start_fen
through tools/legal_moves_runtime fold to one T0122 node while carrying
different contexts (C20 vs A04). 45 contract mutants over all 11 sections;
linkage + registry-invariant drift batteries.

Verifier #1 PASS at cb13858ba8dde0248016427284fc5bf2ffb9726b (v2, two
remediation rounds: end-to-end applied witness + extension-refinement pins;
exact-record atomic merge with registry-snapshot conflicts).
Recorded merge SHA on main: cb13858ba8dde0248016427284fc5bf2ffb9726b
(merge commit f2b18ac).

Status: done
Verification: auto - independent verifier #1 PASS at cb13858ba8dde0248016427284fc5bf2ffb9726b (PR #129); 36-test battery green, full repo suite 2429 passed
Commands: `.venv/bin/python -m pytest tests/test_t0140_opening_context_contract.py -q` (36/36); `.venv/bin/python -m pytest tests/ -q` (2429 passed); `.venv/bin/python tools/opening_context_contract_lint.py` clean. Environment: python 3.10 sandbox venv, pytest, PyYAML, ruff, zstandard; branch feat/t0140-opening-context-contract off main 8ba95d1; main merge commit f2b18ac.
