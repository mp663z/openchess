# T0149 graph provenance contract - evidence

data/contracts/provenance.yaml (schema v1) +
tools/provenance_contract_lint.py +
tests/test_t0149_provenance_contract.py (39-test battery) +
docs/t0149-provenance-design.md. The contract is the source of truth
for graph-record provenance: which source observations produced every
graph record - evidence-grade origin attribution keyed by
target-record identity (never by path, never by digest), additive
only, and the rights attribution surface (fail closed on unknown
sources). Targets are a CLOSED enum (transposition_node, route_edge,
opening_context) with each identity validated THROUGH the owning
sibling's own machinery. Source entries are exactly (source_id from
the linked import registry, game_id verbatim, RFC3339-UTC seconds-Z
timestamp validated as an actual Gregorian calendar instant with a
pinned non-leap-second profile). Merge is ATOMIC insert-or-union
consuming exact validated stored records (never reconstructed):
idempotent/commutative/associative by set union, NO same-key conflict
class (union is total), rejected merges leave the destination
bit-identical over both orders. Totality: explicit field-type guards
before sibling calls; 44-case Cartesian mutation battery; calendar
boundary vectors (leap/non-leap February, 30/31-day months, :60
rejected). Verifier remediation v2 (two blockers) mapped verbatim.

Verifier #1 PASS at 7740fe0ae3b51feed5d290e007108e33f844b615 (v2,
after one remediation round: calendar-exact timestamps + total
target validation).

Recorded merge SHA on main: 7740fe0ae3b51feed5d290e007108e33f844b615

Status: done
Verification: auto - independent verifier #1 PASS at 7740fe0ae3b51feed5d290e007108e33f844b615 (PR #130 v2); battery 39/39, full repo suite 2522 passed / 5 skipped
Commands: `.venv/bin/python -m pytest tests/test_t0149_provenance_contract.py -q` (39/39); `.venv/bin/python -m pytest tests/ -q` (2522 passed, 5 skipped); `.venv/bin/python tools/provenance_contract_lint.py` clean; `.venv/bin/ruff check tools/provenance_contract_lint.py tests/test_t0149_provenance_contract.py` clean. Environment: python 3.10 sandbox venv, pytest, PyYAML, ruff, zstandard; branch feat/t0149-provenance-contract off main 2638b6a; main merge commit 195c389.
