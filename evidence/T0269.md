# T0269 Store/corruption/implement - evidence

Status: done
Verification: auto
Commands: `python -m pytest tests/test_t0269_corruption_runtime.py tests/test_t0268_corruption_red.py` passed at the exact reviewed head; `ruff check` changed files clean; independent verifier #1 and #2 content PASS and FINAL at 40d0fe1, sole parent equal to live main f5b4261; production binding byte-identical to the merged T0268 contract blob; both CI build-test-lint jobs green. Environment: Python 3.12 Linux GitHub Actions and sandbox with pytest, PyYAML, ruff, zstandard.
Mutation: independent verifier #1 ran 62 production mutants: 52 red, 1 red by hang, 9 surviving equivalents: (1) values = value instead of list.__iter__ - no caller code runs during encoding; (2) key isinstance - str-subclass key still rejected when encoded; (3) value.items() instead of dict.items - dict subclasses already rejected by exact type; (4) key encoded at depth instead of depth+1 - non-empty dict at MAX_DEPTH already fails on its value; (5) canonical_encoding(suffix) instead of list(suffix) - suffix already a list; (6) WAL replay without deepcopy - replay never mutates; (7) binary-search midpoint rounding; (8) token match instead of fullmatch and token UTF-8 check dropped - grammar hex-only, byte-exact compare; (9) isinstance(log, list) - list subclass rejected by the encoder with same class. Independent verifier #2 ran 55 production mutants: 49 red, 6 equivalents; 3000-case differential fuzz clean.
Recorded merge SHA on main: 40d0fe1b909f5774fb230678b452cf1e47ab1f9f
