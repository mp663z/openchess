# T0419 Contracts/OpenAPI/contract - evidence

Status: done
Verification: auto
Commands: `python -m pytest tests/test_t0419_openapi_contract.py` passed at the exact reviewed head; `ruff check` changed files clean; independent verifier #1 and #2 PASS at eb54053 under the hostile-type and precedence rules; landed as a patch-identical cherry-pick in batch AF (PR #328), both verifiers delta-confirmed the batch head, fast-forwarded onto main; both CI build-test-lint jobs green on the batch head (runs 36074292906 push, 36074296010 pull_request). Environment: Python 3.12 Linux GitHub Actions and sandbox with pytest, pytest-xdist, PyYAML, ruff, zstandard. Notes: Idempotency-Key nonempty reading pinned from the merged mock; the Python-vs-ECMA whitespace-set difference for that pattern is recorded for the owner.
Mutation: both verifiers ran source-mutant and forged-boundary sweeps at the reviewed head; all findings closed before PASS; surviving mutants ruled equivalent are recorded in the task's requirements file.
Recorded merge SHA on main: 4a2f96cb78b4b96e44a7d9844573a7d182583969
