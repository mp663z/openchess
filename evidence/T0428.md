# T0428 Contracts/events/contract - evidence

Status: done
Verification: auto
Commands: `python3 -m ruff check .`, `python3 -m tools.events_contract_lint`, and `python3 -m pytest -q tests/test_t0428_events_contract.py` (8 passed, 21 subtests) passed at the exact reviewed head; `python3 tools/dag.py verify`, `python3 tools/evidence_lint.py` and `git diff --check` passed in the coordinator workspace. Independent verifier #1 and #2 each reported PASS at be2f0b3c3ab606d97ae49286218a5e21dd739d80 after earlier E501 and joint source/catalog mutant findings were amended. The reviewed commit itself was used in batch AU (PR #349), both verifiers delta-confirmed the exact batch head, and main fast-forwarded after three build-test-lint runs succeeded at that SHA (lane push 36150183677, batch push 36150313971, PR 36150318593). Environment: Python 3.12 Linux GitHub Actions and sandbox with pytest, pytest-xdist, PyYAML, ruff. The coordinator's full local pytest attempt exceeded its 120-second command limit; the three GitHub Actions build-test-lint runs supply the full gate.
Mutation: behavioral rows cover all declared control-plane operations and their permitted outcomes, malformed/hostile types, private extra fields, replay, conflicting duplicate IDs, all-or-none publication failure, and a joint source+catalog addition with a self-updated digest at the unchanged version. The joint mutant is rejected by the independently pinned v1 catalog baseline. The contract claims no shipped event emission or HTTP status mapping, and does not authorize hosted sends or OAuth/sync work.
Recorded merge SHA on main: be2f0b3c3ab606d97ae49286218a5e21dd739d80
