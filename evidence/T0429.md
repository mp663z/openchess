# T0429 Contracts/events/fixture - evidence

Status: done
Verification: auto
Commands: `python3 -m ruff check .`, `python3 -m tools.events_contract_lint`, and `python3 -m pytest -q tests/test_t0429_events_fixture.py tests/test_t0428_events_contract.py` (257 passed, 21 subtests) passed at the reviewed head; DAG verify, evidence lint and diff-check also passed. Independent verifiers #1 and #2 each reported PASS at 1d820311576f8733d7b4b3f7ce66d3c9fec59125; they reproduced and confirmed the T0428 hostile-key reference fix, byte-identical fixture regeneration, all 26 behavioral mutant kills and three equivalent edits. The exact same commit was used in batch AV (PR #351), both verifiers delta-confirmed the batch head, and main fast-forwarded after three build-test-lint CI runs succeeded at that SHA (lane push 36160942215; batch push 36163318081; PR 36163322894). Environment: Python 3.12 Linux GitHub Actions and sandbox with pytest, pytest-xdist, PyYAML, ruff. Full local suite was unverified due to the command time limit; the three CI build-test-lint runs supplied the full gate.
Mutation: the fixture contains happy outcomes across the pinned 14-operation catalog, boundary, malformed and rollback rows, with a closed manifest and deterministic synthetic IDs. Every one of the 26 claimed reference mutants turned behavioral rows red; three equivalent edits remained green. The separate T0428 reference amendment checks exact string keys before set algebra, refusing hostile subclasses without invoking user code. This is contract/fixture work, not a shipped event emitter or hosted request.
Recorded merge SHA on main: 1d820311576f8733d7b4b3f7ce66d3c9fec59125
