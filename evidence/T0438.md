# T0438 Contracts/versioning/fixture - evidence

Status: done
Verification: auto - fixture acceptance checked at exact landed head 6faf9dc35c1c591077a6b81ddadf2b42e8d90426. Two independent adversarial verifiers reported PASS at that head after a harness malformed-mutation-spec fix: verifier #1 checked typed refusal of null, scalar and list mutation specs and byte-identical generation; verifier #2 confirmed unchanged corpus, authored expectations and behavioral mutant coverage carried forward. PR #368 fast-forwarded to main only after exact-head push and pull-request full build-test-lint CI runs both succeeded (36237338263 and 36237340528). This is an auto-mode task; the independent checks are additional assurance, not a claimed product runtime.
Commands: `python3 -m pytest -q tests/test_t0438_versioning_fixture.py tests/test_t0437_control_plane_versioning_contract.py` (204 passed on the final head); `python3 -m ruff check tools/versioning_fixture_generate.py tests/test_t0438_versioning_fixture.py`; `python3 -m ruff format --check tools/versioning_fixture_generate.py tests/test_t0438_versioning_fixture.py`; `python3 tools/dag.py verify`; `python3 tools/evidence_lint.py`; `python3 tools/dag_reconcile.py`; `git diff --check 500b195c4728eaf7dd2daafc5ad9f7b23b0cfdd8 6faf9dc35c1c591077a6b81ddadf2b42e8d90426`. Environment: Python 3.12 Linux GitHub Actions with pytest, pytest-xdist, PyYAML and ruff; sandbox Python 3 with the same tools.
Mutation: `tests/fixtures/control-plane-versioning/cases.json` has 82 manifest-bound, deterministic rows: 19 happy, 7 boundary, 17 incompatible and 39 malformed. The generator authors expectations from `data/contracts/control_plane_versioning.yaml`, without running the reference; `tests/test_t0438_versioning_fixture.py` materializes old/new snapshots from a pinned base and mutation lists and executes all rows against the T0437 test-only reference. The owning battery kills all 20 reference mutants by executed fixture-row failures and keeps eight strict-validation-preempted equivalent edits green. No production compare or hosted sync is shipped here. Local full xdist runs were non-green due to worker crashes mid-test on T0268, then T0205, with root cause unproven; isolated files passed but are not a full local gate. Both final-head full GitHub CI runs passed. No visual artifact is involved.
Recorded merge SHA on main: 6faf9dc35c1c591077a6b81ddadf2b42e8d90426
