# T0440 Contracts/versioning/implement - evidence

Status: done
Verification: auto - the independent shipped `server/control_plane_versioning.py` compare and VersionError were accepted at exact landed head 50cf1fef6697df5a67a072c79a53fe8d28f46296. Two independent adversarial verifiers reported PASS at that head after review of production source validation, hostile inputs, additive/breaking shape semantics, the T0439 production binding and two correction rounds. PR #372 fast-forwarded to main only after exact-head push and pull-request build-test-lint CI runs both succeeded (36253878347 and 36253882007). The production module imports no tests or fixture oracle.
Commands: `python3 -m pytest -q tests/test_t0437_control_plane_versioning_contract.py tests/test_t0438_versioning_fixture.py tests/test_t0439_versioning_red.py tests/test_t0440_control_plane_versioning.py` (260 passed at the final package head); `python3 -m ruff check server/control_plane_versioning.py tests/test_t0439_versioning_red.py tests/test_t0440_control_plane_versioning.py`; `python3 -m ruff format --check server/control_plane_versioning.py tests/test_t0439_versioning_red.py tests/test_t0440_control_plane_versioning.py`; `python3 tools/dag.py verify`; `python3 tools/evidence_lint.py`; `python3 tools/dag_reconcile.py`; `git diff --check 50048a6f546ec6af6ce45629e7f0667b8ca8d8f9 50cf1fef6697df5a67a072c79a53fe8d28f46296`. Environment: Python 3.12 Linux GitHub Actions with pytest, pytest-xdist, PyYAML, ruff and zstandard; sandbox Python 3 with those tools for targeted checks. Full local xdist gate and standalone T0419 were uncompleted due sandbox command timeouts; no local full-suite pass is claimed. Both final-head CI full gates completed successfully.
Mutation: the T0439 `PRODUCTION_BINDING` now points to the independently implemented shipped compare and error class; all 82 closed T0438 fixture rows, deterministic/pure/hostile probes, nine faulty bindings and 20 T0437 source mutants stayed red where required, and eight preempted equivalents stayed green. Production validates both source snapshots and minors before comparison, distinguishes malformed requests from valid incompatibility, handles aliases while refusing cycles and hostile subclasses, compares deep finite opaque metadata iteratively, and preserves identical-document verdicts for same-position opaque NaN. Commit 776e50c separately corrected a pre-existing T0439 test premise: T0419 accepts deep finite opaque metadata, so the old 1100-deep typed-refusal probe was removed and 600/1100/1400-depth valid incompatible False rows were pinned; cyclic and subclass probes remain typed. The new production test file's introduced format drift was cleaned before final review. The adjudicated same-major reading treats opaque scalar type drift (1 to 1.0; True to 1) as incompatible, while the narrower T0437 test reference uses Python scalar equality and may return True outside the fixture; the contract-first production reading was reviewed, not silently merged with the reference. No hosted-sync or OAuth behavior is shipped. No visual artifact is involved.
Recorded merge SHA on main: 50cf1fef6697df5a67a072c79a53fe8d28f46296
