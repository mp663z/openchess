# T0636 Import/folder/contract test - evidence

Status: done
Verification: auto
Commands: `python -m pytest tests/test_t0636_import_folder_contract.py` passed at the exact reviewed head; `ruff check` changed files clean; independent verifier #1 and #2 each reported PASS at 880a63f under the hostile-type and precedence rules; landed in batch AR (PR #344), both verifiers delta-confirmed the batch head, fast-forwarded onto main; both CI build-test-lint jobs green on the batch head (runs 36093201660 push, 36093204872 pull_request). Environment: Python 3.12 Linux GitHub Actions and sandbox with pytest, pytest-xdist, PyYAML, ruff, zstandard. Notes: Malformed games are rejected and the import continues; rejection record {file, game_number, line, code}, per-file summary uses T0537 counts and the total is their sum, no per-game event for cross-file duplicate/update, folder order and non-PGN/subdirectory handling fail closed; all flagged for owner review.
Mutation: both verifiers ran source-mutant and forged-boundary sweeps at the reviewed head; all findings closed before PASS; surviving mutants the builder declared equivalent are recorded in the task's requirements file; any further equivalents ruled by a verifier are recorded in that verifier's verdict.
Recorded merge SHA on main: 880a63f4a2aca4b0faa074347062a16890f73ae6
