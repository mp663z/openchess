# T0647 Import/cancel/contract test - evidence

Status: done
Verification: auto
Commands: `python -m pytest tests/test_t0647_import_cancel_contract.py` passed at the exact reviewed head; `ruff check` changed files clean; independent verifier #1 and #2 each reported PASS at 598a64e under the hostile-type and precedence rules; landed in batch AT (PR #346), both verifiers delta-confirmed the batch head, fast-forwarded onto main; both CI build-test-lint jobs green on the batch head (runs 36095559366 push, 36095562156 pull_request). Environment: Python 3.12 Linux GitHub Actions and sandbox with pytest, pytest-xdist, PyYAML, ruff, zstandard. Notes: Games committed before the cancel point stay and nothing at or after it commits; the cancel trigger is a test seam, not an API; a run that refuses before the cancel point follows the refusal reference; in a cancelled folder run, whether an earlier rejection record persists and whether game_rejected is emitted is open for owner review.
Mutation: both verifiers ran source-mutant and forged-boundary sweeps at the reviewed head; all findings closed before PASS; surviving mutants the builder declared equivalent are recorded in the task's requirements file; any further equivalents ruled by a verifier are recorded in that verifier's verdict.
Recorded merge SHA on main: 598a64e55a2d666ee0421df858dcdfa4a68f64fe
