# T2232 Behavioral label - evidence
Deliverable: backtest/labels.py. useful = accepted change payload hash still present in the >=30-day snapshot; returns None (unknown) when no post-window snapshot exists - never guessed. Proxy labels (engine_reanalysis, human_panel) are a separate dataclass/kind. tests/test_labels.py.
