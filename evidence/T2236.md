# T2236 Calibration - evidence
Deliverable: backtest/calibration.py. Deterministic seeded split BY REPERTOIRE (sha256-keyed), held-out fraction default 0.2; check_no_leak raises CalibrationLeakError if any held-out repertoire touches calibration. tests/test_calibration.py: coverage, disjointness, determinism per seed, leak guard.
