# T2715 Schema adapters - evidence

Implementation: ingest/schemas.py
- Versioned Normalized envelope (schema_name, schema_version=1) for game/puzzle/eval rows.
- adapt_game_pgn: preserves ALL PGN headers verbatim + identifier subset (Site/White/Black/UTCDate/UTCTime).
- adapt_puzzle_csv_line: validated against the real lichess_db_puzzle.csv field order (fixture row 00008).
- adapt_eval_jsonl: validated against the real lichess_db_eval.jsonl shape (fen + evals[].knodes/depth/pvs cp/mate/line).
- License handling is fail-closed: only known license values (CC0) accepted; anything else raises.
- Malformed records raise ValueError; no silent repair (pairs with T2717 quarantine).

Acceptance -> tests: tests/test_schema_adapters.py (5 tests).
Commands: `pytest tests/test_schema_adapters.py -q`. Environment: python 3.11, stdlib only.
