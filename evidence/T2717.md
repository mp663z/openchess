# T2717 Malformed-record quarantine - evidence

Implementation: ingest/quarantine.py
- QuarantineStore: JSONL sidecar with source_file, byte_offset (decompressed stream), sequence_no, error, verbatim raw bytes.
- process_with_quarantine: handler exceptions quarantine; valid rows continue.
- iter_pgn_games_with_offsets: streaming iterator exposing exact decompressed byte offsets.

Acceptance -> tests (tests/test_quarantine.py):
- offsets match the decompressed stream byte-for-byte (test_offsets_match_decompressed_stream)
- bad rows quarantined, valid rows continue (test_bad_rows_quarantined_valid_continue: 3 ok / 2 quarantined interleaved)
- no silent repair: quarantined raw equals source bytes at the recorded offset (test_no_silent_repair_preserves_raw)

Commands: `pytest tests/test_quarantine.py -q`. Environment: python 3.11, zstandard 0.25.0, stdlib json/sqlite3.

---
pre-contract: true - indexed under the evidence contract (T0007); structured fields not captured at the time. Verification: auto.
