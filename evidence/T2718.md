# T2718 Deduplication - evidence

Implementation: ingest/dedup.py
- Identity rules: game = Site URL (fallback: White|Black|UTCDate|UTCTime|movetext hash); puzzle = PuzzleId; eval = fen.
- Duplicates: skipped, provenance table extended (every source_file retained).
- Conflicting versions: first retained (deterministic), conflict recorded with retained+rejected payloads and hashes; nothing silently overwritten.

Acceptance -> tests (tests/test_dedup.py):
- duplicate fixture: one record, two provenances (test_game_duplicate_keeps_one_row_and_both_provenances)
- conflicting-version fixture: conflict recorded, first version retained, both provenances (test_puzzle_conflicting_version_recorded_with_provenance)
- eval identity + duplicate (test_eval_identity_is_fen); anon-game identity fallback (test_game_without_site_uses_player_date_movetext_identity)

Commands: `pytest tests/test_dedup.py -q`. Environment: python 3.11, stdlib only.
