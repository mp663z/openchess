# T2720 Evaluation calibration - evidence

Implementation: ingest/eval_calibration.py
- Orientation: cp/mate kept in side-to-move perspective; white_perspective_cp provides single-orientation view (flip when black to move).
- Mate encoding: signed mate-in-N validated (mate 0 rejected), folded onto cp scale at MATE_CP_BASE=100,000 (faster mate > slower mate), never mixed with cp (exactly-one-of enforced).
- Depth/knodes: verbatim + quality tiers deep/standard/shallow.
- Engine/version carried through from the record.

Acceptance -> tests (tests/test_eval_calibration.py, 7 tests) against known positions (startpos both orientations, mate/mated cases).
Commands: `pytest tests/test_eval_calibration.py -q`. Environment: python 3.11, stdlib only.

---
pre-contract: true - indexed under the evidence contract (T0007); structured fields not captured at the time. Verification: auto.
