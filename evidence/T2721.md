# T2721 Puzzle calibration - evidence

Implementation: ingest/puzzle_calibration.py
- Themes verbatim (order preserved, no inference), rating + deviation with stable/provisional/volatile tiers, popularity range-checked (-100..100), GameUrl parsed to (game_id, color, ply) provenance, solution lines validated as complete UCI sequences.
- Out-of-semantics rows (bad popularity, negative deviation, non-UCI moves, foreign URLs) rejected, never repaired.

Acceptance -> tests (tests/test_puzzle_calibration.py, 4 tests incl. a real lichess_db_puzzle.csv row).
Commands: `pytest tests/test_puzzle_calibration.py -q`. Environment: python 3.11, stdlib only.
