# T2723 Independent corpus audit - evidence (sample stage, v2)

Audit material (real run, not synthetic):
- Source: data/raw/lichess_db_puzzle.csv.zst (CC0, pinned sha256 95fd454b... in data/datasets/snapshot-pins.yaml).
- Tool: tools/corpus_audit_sample.py streams the zst, normalizes through ingest.schemas.adapt_puzzle_csv_line, reconciles counts/checksums/licenses.
- Result manifest: data/datasets/corpus-audit-sample.json - 20,000 rows read, 20,000 normalized, 0 quarantined, 0 duplicate puzzle IDs, license tally {CC0: 20000}.
- Checksums, described precisely:
  - raw_sample_sha256 46ed896bdab1c4c0c8c0b245f6ec750c5acc2642a97e76320950e13f74c1c225 - checksum of the DECOMPRESSED READ WINDOW (whole 1MiB chunks covering row 20,000 plus buffered bytes past it), not an exact-row checksum. Exactness lives in the row-level values below.
  - normalized_id_multiset_sha256 03dbe477a359fdfe0fe33370066d3d1bad75ed33da112775672b5a1691dfa171 - canonicalization is sha256 over the newline-joined, sorted puzzle IDs (deterministic multiset; this is the intended canonical form).
- Determinism locked by test (two runs identical); committed manifest compared to tool rerun; evidence-vs-manifest staleness guard added (test_evidence_matches_manifest).

Verifier history: v1 (41ceca48) FAIL - stale multiset hash in this file (copied from the pre-off-by-one-fix run) and imprecise raw-checksum description. v2 corrects both.
Verifier protocol: re-run `PYTHONPATH=. python tools/corpus_audit_sample.py data/raw/lichess_db_puzzle.csv.zst 20000 out.json`, reconcile every field of data/datasets/corpus-audit-sample.json, sample raw-to-normalized rows by puzzle id.
Full-corpus audit (24 monthly dumps, 725.4GB) waits on the pin stream and the T2714 external-worker verdict.
Commands: `pytest tests/test_corpus_audit_sample.py -q`. Environment: python 3.11, zstandard 0.25.0.

---
pre-contract: true - indexed under the evidence contract (T0007); structured fields not captured at the time. Verification: independent verifier.
