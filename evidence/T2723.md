# T2723 Independent corpus audit - evidence (sample stage)

Audit material (real run, not synthetic):
- Source: data/raw/lichess_db_puzzle.csv.zst (CC0, pinned sha256 95fd454b... in data/datasets/snapshot-pins.yaml).
- Tool: tools/corpus_audit_sample.py streams the zst, normalizes through ingest.schemas.adapt_puzzle_csv_line, reconciles counts/checksums/licenses.
- Result manifest: data/datasets/corpus-audit-sample.json - 20,000 rows read, 20,000 normalized, 0 quarantined, 0 duplicate puzzle IDs, license tally {CC0: 20000}, raw sample sha256 46ed896b..., normalized multiset sha256 28ba4598....
- Determinism locked by test (two runs identical) and committed-manifest-equals-rerun test.

Verifier protocol: re-run `PYTHONPATH=. python tools/corpus_audit_sample.py data/raw/lichess_db_puzzle.csv.zst 20000 out.json` and reconcile every field; sample raw-to-normalized rows by id.
Full-corpus audit (all 24 monthly dumps) waits on the pin stream (in progress) and heavy compute (T2714 verdict: external worker).
Commands: `pytest tests/test_corpus_audit_sample.py -q`. Environment: python 3.11, zstandard 0.25.0.
