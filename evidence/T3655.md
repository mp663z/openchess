# T3655 Component inventory / release SBOM - evidence

Generator: tools/component_inventory.py; output: docs/component-inventory.json (regenerable, byte-compared in tests).

## v4 (correct wheel-tag matching + constraint validation) - remediation of independent-review FAIL at 7e13f15d

FAIL findings addressed:
1. Fatal: the cp312-win_amd64 target locked three 32-bit win32 wheels (pyyaml, ruff, zstandard) because _tag_ok accepted win32 for the win_amd64 target and _pick_artifact took the first match. Fix: the win_amd64 target accepts ONLY win_amd64 + any platform tags, never win32; lock regenerated (all three now cp312 win_amd64 or py3-none wheels).
2. Tests never checked tag-vs-target compatibility. Fix: test_every_wheel_tag_matches_its_named_target parses EVERY locked wheel filename with packaging.utils.parse_wheel_filename and asserts at least one fully compatible tag per target (independent of the tool's own matcher), plus a dedicated assertion that a win_amd64 target never locks a win32-only wheel.
3. Secondary: constraints discovered after a package was resolved were ignored. Fix: every requirement edge is accumulated per package across the whole closure, and after resolution each resolved version is validated against ALL accumulated specifiers (fail-closed RuntimeError on conflict).

Tests now 14 in tests/test_component_inventory.py.

## v3 (per-target locked resolution with artifact hashes) - remediation of independent-review FAIL at 8fcfc350

FAIL findings addressed:
1. colorama had version null masked as "n/a (conditional...)". Fix: colorama is pytest's win32-only dep; it is now genuinely resolved FOR THE WINDOWS TARGET: colorama 0.4.6, exact wheel filename + sha256 + URL from PyPI digests. No null/n/a versions anywhere (asserted by test_every_artifact_is_exact_and_hashed).
2. Markers were not evaluated per target (pip's resolver evaluates PEP 508 markers against the RUNNING interpreter - verified empirically on pip 22 and pip 26: exceptiongroup/tomli leaked into a 3.12 target, colorama never appears). Fix: tools/lock_release.py no longer uses pip's resolver; it resolves against the PyPI JSON API and evaluates every requires_dist marker with packaging.Marker against a constructed per-target environment (sys_platform, python_version, platform_machine, ...). Release targets are explicit: cp312-manylinux_x86_64 and cp312-win_amd64 (project requires-python >= 3.12, CI on 3.12). test_markers_evaluated_per_target asserts colorama is win-only and exceptiongroup/tomli/typing_extensions are absent on both 3.12 targets.
3. Lock was a captured list with no artifact hashes and no CI equality check. Fix: every artifact carries the PyPI-provided sha256 of the exact wheel; new CI step `python tools/lock_release.py --check` re-resolves both targets from PyPI and byte-compares against the committed lock, so requirement edits AND upstream drift fail the build.
4. Requirements stayed broad ranges with drift undetected. Fix: the --check CI gate IS the drift detector - any change in the resolution (new upstream release changing the closed set, requirement edit, manual lock edit) makes the committed lock stale; the lock is also fingerprinted into the SBOM inputs hash (test_lock_is_fingerprinted_into_inputs).

Manifest schema 3: libraries = union over targets; each library records license/origin/direct/requirement plus per-target {version, filename, sha256, url}. Cross-target license mismatches raise at generation time.
Tests (tests/test_component_inventory.py, 13): adds per-target closure assertions, marker-evaluation assertions, exact version/hash assertions, manifest-vs-lock exact version/hash match, on top of the v2 byte-compare and discovery probes.
Commands: `python tools/lock_release.py && python tools/component_inventory.py && pytest tests/test_component_inventory.py -q`; CI: `python tools/lock_release.py --check`.
Verification: auto + independent review - routed with merge SHA.

## v2 (locked resolution, env-independent) - remediation of independent-review FAIL at eb2f4ff9

FAIL findings addressed:
1. inputs_sha256 hashed unpinned requirement RANGES, so resolved versions could drift while CI stayed green.
   Fix: tools/lock_release.py captures the exact resolved dependency set (direct + transitive closure, versions + SPDX-normalized licenses) from the canonical environment into data/release-lock.json, fail-closed on any UNVERIFIED license. inputs_sha256 now hashes requirements*.txt + snapshot-pins.yaml + release-lock.json, so any requirement change without a re-lock fails CI.
2. Staleness test did not compare regenerated vs committed libraries.
   Fix: test_committed_manifest_regenerates_exactly byte-compares the WHOLE manifest; test_libraries_come_from_lock_not_environment asserts the manifest library set equals the lock set exactly.
3. model_weights/bundled_assets were hardcoded empty.
   Fix: discovery from packaging/asset dirs (models/, weights/ for weights; assets/, static/ for bundled assets) with per-file sha256/bytes; test_discovery_picks_up_weights_and_assets proves discovery works by planting probe files (fails if discovery is hardcoded).

Also fixed en route: tests/test_component_inventory.py regenerate() previously ran `git checkout --` on the manifest (tree-mutating test); removed. License strings normalized to SPDX ("MIT License" -> MIT etc.).

Current inventory: 12 libraries (4 direct), stockfish 19 (GPL-3.0, external UCI, not bundled), 5 pinned datasets (sha256 + origin + CC0), 0 model weights, 0 bundled assets.
Tests (tests/test_component_inventory.py, 9): inputs-current, exact regeneration, lock-set equality, lock fingerprint in inputs hash, version/origin/license per library, transitive closure present, dataset hash/origin/license, sections exist, discovery probe.
Commands: `python tools/lock_release.py && python tools/component_inventory.py && pytest tests/test_component_inventory.py -q`.
Verification: auto + independent review - routed with merge SHA.

## v1 (superseded) - merged at eb2f4ff9, failed independent review 2026-09-19

Environment-sourced manifest (importlib.metadata at generation time), inputs-hash staleness only, hardcoded empty weight/asset sections. Findings above.

Recorded merge SHA on main: 21d530c2bafec618d888f90b33a666ef0ab7c2cc
