# T3656 Linkage and deployment classification - evidence

Acceptance: inventory records subprocess, dynamic/static link, source inclusion, browser bundle, server-only use and distribution channel for each component.

Implementation:
- data/linkage-classification.yaml: curated per-component classification with closed enums (linkage: subprocess/dynamic_link/static_link/source_inclusion/data; deployment: ci_only/server_only/cli_tooling/browser_bundle; distribution: not_distributed/distributed_with_app). Fingerprinted into the SBOM inputs hash.
- tools/component_inventory.py joins it into docs/component-inventory.json (schema 4): every library, engine, and dataset carries linkage + deployment + distribution. Fail-closed: an unclassified component or an out-of-enum value raises at generation time.

Current classification (nothing ships yet):
- 9 PyPI libraries: dynamic_link (runtime import; ruff: subprocess), ci_only, not_distributed.
- stockfish 19: subprocess (external UCI process), server_only, not_distributed, GPL-3.0, not bundled.
- 3 datasets (6 pins): data, server_only, not_distributed.
- model weights / bundled assets: none.

Tests (tests/test_linkage_classification.py, 6): every component classified with allowed values; classification file covers exactly the inventory set; stockfish is external subprocess not distributed; no component distributed today; no browser bundle today; generation fails closed without classification.
Commands: `python tools/component_inventory.py && pytest tests/test_linkage_classification.py -q`.
Verification: auto + independent review - routed with merge SHA.
