# T3656 Linkage and deployment classification - evidence

Acceptance: inventory records subprocess, dynamic/static link, source inclusion, browser bundle, server-only use and distribution channel for each component.

## v2 - remediation of independent-review FAIL at ca0ec1ba

FAIL findings addressed:
1. Fatal: discovered model weights/bundled assets bypassed classification (verifier planted assets/probe-review.bin and generate() succeeded). Fix: discovered files now go through the SAME classification lookup + enum validation, keyed by type + relative path (`model_weight:models/x.bin`, `bundled_asset:assets/x.png` under a new `files:` map in data/linkage-classification.yaml); an unclassified discovered file aborts generation. Adversarial tests plant unclassified asset AND weight and assert generate() raises, then classified ones and assert all three fields join with sha256.
2. Accuracy: PyYAML/zstandard are imported at runtime by repo tooling (backtest/scoring.py, ingest/streaming.py, tools/corpus_audit_sample.py); packaging by tools/lock_release.py - reclassified ci_only -> cli_tooling. Stockfish and datasets reclassified server_only -> local_external, matching ADR-0001's local-first/offline-worker deployment. The enum is documented inline in data/linkage-classification.yaml (ci_only / cli_tooling / local_external / server_only / browser_bundle).

Current classification (nothing ships yet):
- 9 PyPI libraries: dynamic_link (ruff: subprocess); cli_tooling (PyYAML, zstandard, packaging) or ci_only (test-only transitive); not_distributed.
- stockfish 19: subprocess, local_external (external UCI process on the user's machine/offline worker), not_distributed, GPL-3.0, not bundled.
- 3 datasets (6 pins): data, local_external (streamed during offline ingest/backtest), not_distributed.
- model weights / bundled assets: none; discovery + fail-closed classification proven by tests.

Tests (tests/test_linkage_classification.py, 10): every component classified with allowed values; classification file covers exactly the inventory set (components by name, files by type:relpath); stockfish external subprocess not distributed; nothing distributed today; no browser bundle today; fail-closed on unclassified component; fail-closed on unclassified discovered weight AND asset; classified discovered files join all fields.
Commands: `python tools/component_inventory.py && pytest tests/test_linkage_classification.py -q`. Environment: python 3.12, PyYAML.
Verification: auto + independent review - routed with merge SHA.

Recorded merge SHA on main: 302926a0e39c7817018239cdc4d693292d18b8ee

## v1 (superseded) - merged at ca0ec1ba, failed independent review 2026-09-19

Classification covered libraries/engines/datasets only; discovered weights/assets bypassed it; ci_only/server_only classes did not reflect actual deployment.
