# importers

Game import connectors (Layer 1: Lichess, Chess.com, unified). Each
connector is a small, separately testable package with a pinned API
contract, rate-limit handling, and a fixture corpus. Rights policy applies
to every fetched artifact (docs/rights-policy.md).
