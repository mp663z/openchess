# T0152 Graph/provenance/implement

## Normative requirements

1. `graph.provenance` must independently implement `ProvenanceError`, `validate_record(record)`, and `ProvenanceTable`; production and its protected property battery must not import or execute `tests.*`, the T0149 interpreter, or fixture helpers.
2. Validation must preserve the T0149 contract's observable precedence: exact record shape, closed target kind, canonical source validation, then linked target validation. Pairwise multi-invalid inputs must return the same closed failure class and code.
3. Target identity must be derived through shipped sibling runtimes. Source entries form a canonical set, and insertion unions sources by exact target identity without storing derived fields.
4. Rejected inserts and hostile table merges must be atomic. Caller records, stored state, and detached values returned by `insert()` and `records()` must not alias one another.
5. The T0151 battery must turn green by replacing only its three oracle bindings with production imports. Its 17 scenarios and assertions must remain unchanged.
6. The protected T0152 property battery and this requirements artifact are part of the production proof. The external dependency lint must reject test-package imports and unapproved dynamic capability access in that battery.

## Requirement-to-test mapping

- R1: `test_r1_r6_property_file_is_externally_dependency_linted` plus the external repository guard.
- R2: `test_r2_pairwise_multi_invalid_precedence`.
- R3: `test_r3_all_sibling_target_kinds_have_stable_exact_identity` and `test_r3_source_union_dedup_order_and_stored_shape_closure`.
- R4: `test_r4_insert_records_and_serialize_are_detached` and `test_r4_hostile_batches_are_atomic_even_on_baseexception`.
- R5: `test_r5_exact_t0151_switch_changes_only_three_bindings`.
- R6: `test_r1_r6_property_file_is_externally_dependency_linted`, `test_repository_guard_lints_protected_property_files_independently`, and `test_fixture_parameters_require_literal_local_nonindirect_parametrize`.

## Red/green transition

On exact parent `e17405de103065bcf703c1191b22d87a84bea69d`, an import-only T0151 switch fails because `graph.provenance` does not exist. At exact implementation head `86824d5c2ded896630abd4a752a2bf93ae3355a3`, the same import-only switch passes all 17 T0151 scenarios with no semantic test changes. This follow-up hardens the independently implemented runtime's validation precedence and production-facing proof without changing that public API.
