# T0180 Graph/diff/unit/property

## Normative requirements

1. Shipped production code must construct exact graph node records and canonical identity keys from a variant and canonical snapshot FEN. No production-property test may depend on `tests.*` helpers.
2. For deterministic seeded pairs of exact-valid states, production diff compute/apply must round-trip both directions, preserve inputs, use canonical section order, and swap added/removed under reversal.
3. Equal states must produce an empty diff with equal canonical state IDs.
4. Well-formed but semantically wrong sibling digests must reject as `malformed_diff_record` on section validation/apply without input mutation.
5. Whole-base mismatch, target-ID divergence, and hostile state containers must reject with the declared typed failure and preserve caller inputs.
6. Under the current exact three-field node schema, changed is future-facing; exact-valid production-generated states have an empty changed section.

## Red/green transition

On the exact parent, the complete T0180 file fails at import because `graph.node` and its production `make_record`/`record_identity` API do not exist. At the reviewed head, the shipped API exists and all requirements above pass.
